---
type: daily-work-plan
status: active
phase: v1
owner: project-lead
updated: 2026-06-05
project_root: D:\腕表工程文件\as腕表ai工作流app开发
tags: [daily-plan, execution, review, tomorrow-plan]
---

# 每日工作推进计划

## 固定流程

每天开工前先读：

- [[Codex底层执行原则]]
- [[每日开工与收工流程]]
- [[完整工作计划]]
- [[周计划]]
- [[明日计划]]
- [[风险与决策]]

每天下班前必须同步：

- 今日完成内容
- 今日未完成内容
- 当前 Git 状态
- 明天第一步
- 明天不要做什么

## 2026-06-05 今日推进记录

### 今日主要目标

- 复核 AI 电商自动化平台 Demo 工程今天的真实工作区状态。
- 判断最近验证结果能否从现有记录中正式确认。
- 同步 Obsidian 收工记录，避免把“脚本存在”误写成“验证已通过”。

### 今日已完成

1. 工程状态复核

- 已确认当前分支仍是：

```text
codex/credential-readiness
```

- 已确认最近已提交基线仍是：

```text
1ae0918 feat: add auth readiness guardrails
450fb64 feat: add demo permission matrix guardrails
6cb96c1 feat: add credential readiness guardrails
2951e5c feat: add sync worker audit sandbox
3bbca84 feat: expose sync audit logs
```

2. 未提交改动范围复核

- 已确认当前仍有 19 个未提交文件：
  - 8 个已跟踪修改。
  - 11 个新增文件。
- `git diff --stat` 结果仍是 8 个已跟踪文件：
  - 新增约 230 行。
  - 删除约 43 行。
- 当前未提交面与 2026-06-04 收工时一致，今天没有新的工程提交收口这批改动。

3. 最近验证留痕边界复核

- 已看到最近与验证相关的文件时间仍集中在 2026-06-03 11:04 至 12:29：
  - 新增 smoke 脚本时间存在。
  - `.next` 构建产物和 `tsconfig.tsbuildinfo` 时间存在。
- 这些时间戳只能证明：
  - 代码和脚本在 2026-06-03 有改动。
  - 本地在 2026-06-03 发生过一次构建或编译痕迹。
- 这些时间戳不能证明以下命令已经正式执行并通过并被可靠记录：

```text
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

4. 收工文档同步

- 已更新：
  - `D:\SHKF\docs\06-项目推进\每日工作推进计划.md`
  - `D:\SHKF\docs\06-项目推进\明日计划.md`
  - `D:\SHKF\docs\06-项目推进\风险与决策.md`
  - `D:\SHKF\docs\06-项目推进\节点审核记录.md`

### 今日未完成

- 还没有拿到 2026-06-03 这批 RBAC / Session 改动的新 smoke 执行结果留痕。
- 还没有重新确认 `typecheck / lint / build / verify-template / git diff --check` 已对当前 19 个未提交文件重跑通过。
- 还没有提交 Git。
- 还没有安装或接入真实登录供应商。
- 还没有把权限来源切换到真实 Session + PostgreSQL RBAC。

### 当前未提交工程文件

```text
README.md
app/api/auth/session-readiness/route.ts
app/api/me/route.ts
lib/permissions.ts
package.json
scripts/seed-support-demo-data.mjs
scripts/smoke-auth-readiness-api.mjs
scripts/smoke-permissions-api.mjs
app/api/auth/session-adapter-readiness/route.ts
app/api/auth/session-context-readiness/route.ts
app/api/permissions/rbac-readiness/route.ts
docs/真实登录Session与权限落地方案.md
lib/auth/session-adapter.ts
lib/auth/session-context.ts
lib/rbac/postgres-rbac-readiness.ts
scripts/smoke-rbac-api.mjs
scripts/smoke-rbac-postgres.mjs
scripts/smoke-session-adapter-api.mjs
scripts/smoke-session-context-api.mjs
```

### 今日验证结果

今天能确认的只有：

- `git status --short` 已核对。
- `git diff --stat` 已核对。
- 当前分支仍是 `codex/credential-readiness`。
- 最近已提交基线仍是 `1ae0918 feat: add auth readiness guardrails`。
- 2026-06-03 的 smoke 脚本、readiness 接口和构建痕迹文件确实存在。

今天不能确认已经通过：

```text
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

结论：

- 今天收工仍只能记录为“状态已复核，验证留痕仍缺”。
- 不能把 2026-06-03 的 RBAC / Session 准备层改动写成“已完成验证”。

### 今日重要决策

- 今天仍不扩功能，只做状态复核和 Obsidian 收口。
- 当前推荐路线继续保持 `Auth.js + PostgreSQL RBAC`，但在 smoke 和完整校验没有正式留痕前，不进入本地提交阶段。
- 明天第一优先级仍是补验证，不是安装真实登录依赖，不是接真实平台。

### 明天第一步

- 先打开：

```text
D:\SHKF\docs\06-项目推进\每日工作推进计划.md
D:\SHKF\docs\06-项目推进\明日计划.md
D:\SHKF\docs\06-项目推进\风险与决策.md
D:\SHKF\docs\06-项目推进\节点审核记录.md
```

- 再执行：

```text
git status --short
git diff --stat
git log --oneline -5
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
```

- 如果 smoke 正常，再继续补：

```text
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

### 明天不要做什么

- 不在验证留痕缺失时提交 Git 或推 GitHub。
- 不直接安装 Auth.js、Clerk、Auth0、NextAuth。
- 不把 readiness 接口说成真实登录、真实 RBAC 或真实客户权限已经上线。
- 不接真实平台。
- 不导入真实用户、真实客户消息或真实密钥。

## 2026-06-04 今日推进记录

### 今日主要目标

- 核对 AI 电商自动化平台 Demo 当前真实状态。
- 确认最近可追溯的验证基线是否仍成立。
- 同步 Obsidian 收工记录，避免把 2026-06-03 的未验证改动误写成已完成。

### 今日已完成

1. 工程状态核对

- 已确认当前分支仍是：

```text
codex/credential-readiness
```

- 已确认最近已提交基线仍是：

```text
1ae0918 feat: add auth readiness guardrails
450fb64 feat: add demo permission matrix guardrails
6cb96c1 feat: add credential readiness guardrails
2951e5c feat: add sync worker audit sandbox
3bbca84 feat: expose sync audit logs
```

2. 未提交改动范围核对

- 已确认当前仍有 19 个未提交文件：
  - 8 个已跟踪修改。
  - 11 个新增文件。
- `git diff --stat` 只能确认已跟踪文件新增约 230 行、删除约 43 行。
- 最新改动时间集中在 2026-06-03 11:04 至 12:27，说明今天没有新增工程代码提交，今天主要完成的是基线审计和收工同步。

3. 最近验证记录边界核对

- 已能从 Obsidian 记录确认的最新完整验证基线，仍停留在 2026-06-02 已提交节点。
- 2026-06-03 新增的以下入口：
  - `npm run smoke:rbac`
  - `npm run smoke:rbac:postgres`
  - `npm run smoke:session-adapter`
  - `npm run smoke:session-context`
- 当前只能确认“脚本和接口已落地”，不能确认“已经实际执行并通过”。

4. 收工文档同步

- 已更新：
  - `D:\SHKF\docs\06-项目推进\每日工作推进计划.md`
  - `D:\SHKF\docs\06-项目推进\明日计划.md`
  - `D:\SHKF\docs\06-项目推进\风险与决策.md`
  - `D:\SHKF\docs\06-项目推进\节点审核记录.md`

### 今日未完成

- 还没有运行并留痕 2026-06-03 新增的 RBAC / Session 烟测。
- 还没有重新确认这批改动的 `typecheck / lint / build / verify-template / git diff --check` 结果。
- 还没有提交 Git。
- 还没有安装或接入真实登录供应商。
- 还没有把权限来源切换到真实 Session + PostgreSQL RBAC。

### 当前未提交工程文件

```text
README.md
app/api/auth/session-readiness/route.ts
app/api/me/route.ts
lib/permissions.ts
package.json
scripts/seed-support-demo-data.mjs
scripts/smoke-auth-readiness-api.mjs
scripts/smoke-permissions-api.mjs
app/api/auth/session-adapter-readiness/route.ts
app/api/auth/session-context-readiness/route.ts
app/api/permissions/rbac-readiness/route.ts
docs/真实登录Session与权限落地方案.md
lib/auth/session-adapter.ts
lib/auth/session-context.ts
lib/rbac/postgres-rbac-readiness.ts
scripts/smoke-rbac-api.mjs
scripts/smoke-rbac-postgres.mjs
scripts/smoke-session-adapter-api.mjs
scripts/smoke-session-context-api.mjs
```

### 今日验证结果

今天能确认的只有：

- `git status --short` 与 `git diff --stat` 已核对。
- 当前分支仍是 `codex/credential-readiness`。
- 最近已提交基线仍是 `1ae0918 feat: add auth readiness guardrails`。
- 2026-06-03 的新增文件和脚本确实存在于工程中。

今天不能确认已经通过：

```text
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

结论：

- 今天收工只能记录为“状态已核对，验证待补”。
- 不能把 2026-06-03 的准备层改动写成“已经验证通过”。

### 今日重要决策

- 今天不扩功能，不制造新的未提交面，只做基线核对和 Obsidian 收口。
- 当前路线继续保持 `Auth.js + PostgreSQL RBAC`，但在新增 smoke 和完整校验没有留痕前，不进入本地提交阶段。
- 明天优先补验证，不优先装真实登录依赖，不优先接真实平台。

### 明天第一步

- 先打开：

```text
D:\SHKF\docs\06-项目推进\每日工作推进计划.md
D:\SHKF\docs\06-项目推进\明日计划.md
D:\SHKF\docs\06-项目推进\风险与决策.md
D:\SHKF\docs\06-项目推进\节点审核记录.md
```

- 再执行：

```text
git status --short
git diff --stat
git log --oneline -5
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
```

- 如果 smoke 正常，再继续补：

```text
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

### 明天不要做什么

- 不在验证缺失时提交 Git 或推 GitHub。
- 不直接安装 Auth.js、Clerk、Auth0、NextAuth。
- 不把 readiness 接口说成真实登录、真实 RBAC 或真实客户权限已经上线。
- 不接真实平台。
- 不导入真实用户、真实客户消息或真实密钥。

## 2026-06-03 今日推进记录

### 今日主要目标

- 继续推进真实登录前置准备，不接真实登录供应商。
- 把 PostgreSQL RBAC、Session 适配层、Session 上下文准备状态补成可检查接口和烟测入口。
- 同步 Obsidian 收工记录，避免明天开工误判基线。

### 今日已完成

1. PostgreSQL RBAC 准备状态

- 已新增：
  - `GET /api/permissions/rbac-readiness`
  - `lib/rbac/postgres-rbac-readiness.ts`
  - `scripts/smoke-rbac-api.mjs`
  - `scripts/smoke-rbac-postgres.mjs`
- 目标是只读检查本地 PostgreSQL 假数据中的角色、权限、租户成员是否齐全，并对照当前静态权限矩阵。

2. Session 适配层与上下文准备

- 已新增：
  - `GET /api/auth/session-adapter-readiness`
  - `GET /api/auth/session-context-readiness`
  - `lib/auth/session-adapter.ts`
  - `lib/auth/session-context.ts`
  - `scripts/smoke-session-adapter-api.mjs`
  - `scripts/smoke-session-context-api.mjs`
- 已增强：
  - `GET /api/auth/session-readiness`
  - `GET /api/me`
- 当前方向明确为：
  - 推荐路线：`Auth.js + PostgreSQL RBAC`
  - 先锁定接口和权限边界
  - 仍不安装真实登录依赖
  - 仍不接真实客户账号

3. 权限矩阵与种子数据补强

- 已更新：
  - `lib/permissions.ts`
  - `scripts/seed-support-demo-data.mjs`
  - `scripts/smoke-auth-readiness-api.mjs`
  - `scripts/smoke-permissions-api.mjs`
  - `package.json`
- 已新增本地方案文档：
  - `docs/真实登录Session与权限落地方案.md`
- `package.json` 已补脚本：
  - `npm run smoke:rbac`
  - `npm run smoke:rbac:postgres`
  - `npm run smoke:session-adapter`
  - `npm run smoke:session-context`

### 今日未完成

- 还没有提交 Git。
- 还没有把今天这批 RBAC / Session 改动跑出可确认的完整验证记录。
- 还没有安装或接入真实登录供应商。
- 还没有把权限来源正式切换到真实 Session + PostgreSQL RBAC，仅完成准备层和自检层。

### 当前未提交工程文件

```text
README.md
app/api/auth/session-readiness/route.ts
app/api/me/route.ts
lib/permissions.ts
package.json
scripts/seed-support-demo-data.mjs
scripts/smoke-auth-readiness-api.mjs
scripts/smoke-permissions-api.mjs
app/api/auth/session-adapter-readiness/route.ts
app/api/auth/session-context-readiness/route.ts
app/api/permissions/rbac-readiness/route.ts
docs/真实登录Session与权限落地方案.md
lib/auth/session-adapter.ts
lib/auth/session-context.ts
lib/rbac/postgres-rbac-readiness.ts
scripts/smoke-rbac-api.mjs
scripts/smoke-rbac-postgres.mjs
scripts/smoke-session-adapter-api.mjs
scripts/smoke-session-context-api.mjs
```

### 今日验证结果

已能从代码和脚本确认：

- 今日新增 RBAC / Session 方向的接口、帮助模块和烟测脚本已经落到工程里。
- 当前分支仍是：

```text
codex/credential-readiness
```

- 最近已确认提交基线仍是：

```text
1ae0918 feat: add auth readiness guardrails
```

尚不能从现有记录确认今天已经执行并通过：

```text
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
npm run smoke:auth-readiness
npm run smoke:permissions
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

说明：

- Obsidian 当前没有今天这批新改动对应的验证通过记录。
- 仓库当前有未提交改动，且没有看到今天新的完整校验结论。
- 因此今天收工只能记录“方向推进正确，但校验待补”，不能写成已通过。

### 今日重要决策

- 真实登录方案先锁定为“推荐 `Auth.js + PostgreSQL RBAC`”，但今天不安装依赖、不接真实供应商。
- 业务 API 继续统一走请求上下文，不能让未来真实登录接入后再到处散读 Cookie、请求头或前端自报角色。
- PostgreSQL RBAC 只做本地假数据只读校验，不导入真实用户，不导入真实客户数据。
- 今天不提交 Git，不推 GitHub，先补齐验证和收工记录。

### 明天第一步

- 先打开：

```text
D:\SHKF\docs\06-项目推进\每日工作推进计划.md
D:\SHKF\docs\06-项目推进\明日计划.md
D:\SHKF\docs\06-项目推进\风险与决策.md
D:\SHKF\docs\06-项目推进\节点审核记录.md
```

- 再检查工程：

```text
git status --short
git diff --stat
git log --oneline -5
```

- 然后只做一件事：先补今天这批 RBAC / Session 改动的完整验证记录。

建议优先执行：

```text
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
npm run smoke:auth-readiness
npm run smoke:permissions
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

### 明天不要做什么

- 不直接安装 Auth.js、Clerk、Auth0、NextAuth 等真实登录依赖。
- 不把今天的准备层接口说成“真实登录已完成”。
- 不接真实平台。
- 不导入真实用户、真实客户消息或真实密钥。
- 不在验证缺失的状态下提交 Git 或推 GitHub。

## 2026-06-01 今日推进记录

### 今日主要目标

- 用 Figma 优化「数据接入」页面。
- 将 Figma 方向同步到当前 Demo 工程。
- 检查哪些修改不如早期版本。
- 恢复重要业务判断信息，尤其是风险等级颜色。
- 把明天继续开发的计划同步到 Obsidian。

### 今日已完成

1. Figma 设计稿

- 已创建 Figma 文件：`AI电商自动化后台_数据接入页设计稿`
- 有效画板：`AI电商自动化后台 / 数据接入 / v2 可交付设计稿`
- 链接：https://www.figma.com/design/0xedkjEVOErDB6bjT3o2n2?node-id=3-2

2. 数据接入页优化

- 完成 6 个数据接入卡片布局：
  - Shopify / 独立站
  - Meta Ads
  - TikTok Ads
  - Instagram Graph
  - 物流 API
  - 客服系统
- 保留 2 行 3 列结构。
- 增加 CSV 数据上传模块。
- 更新输入框示例。
- 右上角按钮调整为：
  - `连接平台`：主按钮
  - `保存连接`：次按钮

3. 已发现并修正的倒退点

- 之前过度追求极简，弱化了后台业务判断。
- 已恢复风险等级颜色：
  - 低风险：绿色
  - 中风险：橙色
  - 高风险：红色
- 已恢复数据接入页左侧安全边界：
  - Demo / 真实模式
  - 岗位权限
  - 今日闭环
- 已恢复接入卡片状态：
  - 未填写：`Demo`
  - 填写保存后：`已连接`

### 当前未提交代码文件

当前 Demo 工程还有未提交改动，明天先审查，不要马上提交。

```text
D:\腕表工程文件\as腕表ai工作流app开发\app\globals.css
D:\腕表工程文件\as腕表ai工作流app开发\components\growth-app.tsx
D:\腕表工程文件\as腕表ai工作流app开发\index.html
D:\腕表工程文件\as腕表ai工作流app开发\lib\demo-data.ts
D:\腕表工程文件\as腕表ai工作流app开发\lib\types.ts
D:\腕表工程文件\as腕表ai工作流app开发\src\app.js
D:\腕表工程文件\as腕表ai工作流app开发\src\app.ts
D:\腕表工程文件\as腕表ai工作流app开发\src\styles.css
```

### 今日验证结果

已通过：

```text
node --check src/app.js
node scripts/verify-template.mjs
git diff --check
```

未通过：

```text
npm run typecheck
```

原因：

- 当前项目没有安装 `node_modules`。
- 本地没有可用的 `tsc`。
- 这不是今天页面代码本身的语法错误。

### 今日重要决策

- 后台页面不能只追求“干净”和“高级”。
- 商用后台必须优先保证：
  - 风险等级清楚
  - 权限边界清楚
  - Demo / 真实模式清楚
  - 连接状态清楚
  - AI 自动化边界清楚
- Figma 只作为视觉质量参考，不能替代业务判断。

### 今日不要继续做的事

- 不提交 Git。
- 不推 GitHub。
- 不接真实 API。
- 不填写真实密钥。
- 不让 AI 自动发送真实客户消息。
- 不删除现有 Demo。

## 明日计划入口

明天直接打开：

- [[明日计划]]
- [[2026-06-01-数据接入页与风险颜色审查记录]]

## 2026-06-02 今日推进记录

### 今日主要目标

- 暂停 UI 视觉打磨。
- 先冻结昨天 Demo/UI 状态，避免后续工程开发混乱。
- 开始推进 AI 客服消息中台工程底座。
- 补数据库模型、类型、模拟数据、权限和 API 骨架。
- 同步 Obsidian，方便明天继续。

### 今日已完成

1. 本地 checkpoint

- 已通过提交前基础验证。
- 已创建本地 Git checkpoint：

```text
830b459 chore: checkpoint demo state before backend foundation
```

- 该提交只在本地，不推 GitHub。

2. 数据库草案

- 已在 `db/schema.sql` 增加 AI 客服消息中台一期表：
  - `support_channels`
  - `support_channel_accounts`
  - `customers`
  - `customer_identities`
  - `customer_threads`
  - `messages`
  - `message_translations`
  - `ai_reply_suggestions`
  - `ai_autoreplies`
  - `ai_approvals`
  - `handoff_reports`

3. 类型和模拟数据

- 已在 `lib/types.ts` 增加客服会话、消息、翻译、AI 草稿、审批记录和离线日报类型。
- 已在 `lib/demo-data.ts` 增加模拟客户、客户身份、统一会话、消息、翻译、AI 草稿、AI 审批记录和离线托管日报。

4. 权限和托管规则

- 已在 `lib/permissions.ts` 增加：
  - `support.read`
  - `support.reply`
  - `support.autopilot.manage`
- 已新增 `lib/workflows/support.ts`，集中处理客服消息风险判断和 AI 回复草稿生成。
- 高风险退款、投诉、取消订单等内容只生成待审核草稿，不自动发送。

5. API 骨架

- 已新增模拟接口：
  - `GET /api/support/threads`
  - `POST /api/support/threads`
  - `GET /api/support/threads/[threadId]`
  - `POST /api/support/ai-drafts`
  - `GET /api/support/handoff-report`

### 今日验证结果

已通过：

```text
node --check src/app.js
node scripts/verify-template.mjs
git diff --check
```

未执行完整 TypeScript 校验：

```text
npm run typecheck
```

原因：

- 当前项目没有 `node_modules`。
- 本地没有项目内 `tsc`。
- 明天如果要进入 Next.js 后端正式开发，应先安装依赖再跑完整校验。

### 当前未提交工程文件

```text
db/schema.sql
lib/demo-data.ts
lib/permissions.ts
lib/types.ts
app/api/support/ai-drafts/route.ts
app/api/support/handoff-report/route.ts
app/api/support/threads/[threadId]/route.ts
app/api/support/threads/route.ts
lib/workflows/support.ts
```

### 今日重要决策

- 今天开始从 UI 暂停，转向工程底座。
- AI 客服一期先做模拟消息中台，不接真实 WhatsApp、抖音、企业微信或个人微信。
- AI 只能生成草稿、翻译、总结、低风险引导和离线日报。
- 高风险客户消息必须人工审核，不能自动发送。
- 后续真实模式必须写入数据库、审计日志和 AI 审批记录。

### 明天第一步

- 先审查今天新增的客服消息中台工程骨架。
- 再决定是否安装依赖并执行：

```text
npm run typecheck
npm run lint
npm run build
```

- 验证通过后，再决定是否提交本轮工程底座改动。

## 2026-06-02 工程校验收口记录

### 本次目标

- 安装项目依赖。
- 跑完整 TypeScript、Lint、Build 校验。
- 修复校验问题。
- 对客服消息中台 API 做烟测。
- 校验通过后提交本地 Git。

### 已完成

1. 依赖安装

- 已执行：

```text
npm install
```

- 已生成：

```text
package-lock.json
```

2. 校验修复

- 已修复 BullMQ 和 ioredis 类型冲突。
- 已新增 ESLint CLI 配置：

```text
eslint.config.mjs
```

- 已把 `package.json` 的 lint 脚本从 `next lint` 改为：

```text
eslint .
```

- 已清理未使用图标导入。
- 已在 `.gitignore` 增加 `tsconfig.tsbuildinfo`，避免 TypeScript 缓存污染 Git。

3. 完整校验

已通过：

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node scripts/verify-template.mjs
git diff --check
```

4. API 烟测

本地服务：

```text
http://127.0.0.1:4173
```

已验证：

- `GET /api/support/threads` 正常返回统一客服会话。
- `POST /api/support/ai-drafts` 对退款类消息返回高风险草稿。
- 高风险草稿 `canAutoSend` 为 `false`。
- `GET /api/support/handoff-report` 正常返回离线托管日报。
- GM 角色调用客服回复草稿接口被 403 拦截。

5. 本地 Git 提交

已提交：

```text
565f096 feat: add support message hub backend skeleton
```

当前 Git 状态：

```text
干净，无未提交代码改动。
```

### 安全与风险记录

- 没有接真实 API。
- 没有写入真实 token、secret、邮箱密码。
- 没有自动发送真实客户消息。
- `npm audit` 仍提示 Next 依赖链中 PostCSS 有 2 个中危风险，但 npm 建议的 `--force` 修复会降级 Next，不适合执行。

### 下一步建议

下一步进入：

```text
统一客服工作台功能闭环
```

重点不是继续调 UI，而是让页面从静态 Demo 改为读取客服 API：

- 读取统一客服会话列表。
- 打开会话详情。
- 生成 AI 回复草稿。
- 显示风险等级和是否允许自动发送。
- 显示离线托管日报。
- 高风险只允许人工审核，不允许自动发送。

## 2026-06-02 统一客服工作台功能闭环记录

### 本次目标

- 暂停 UI 视觉打磨，继续推进项目工程底座。
- 把统一客服工作台从静态展示推进到读取 Demo API。
- 保留 Demo/真实模式边界，不能误导为已经接通真实平台。
- 按要求补齐页面中必要英文术语的中文解释。

### 已完成

1. 统一客服工作台接入 Demo API

- 已接入：

```text
GET /api/support/threads
GET /api/support/threads/[threadId]
POST /api/support/threads
POST /api/support/ai-drafts
GET /api/support/handoff-report
```

- 页面现在可以显示：
  - 会话列表。
  - 会话详情。
  - 客户原文。
  - 中文翻译。
  - AI 回复草稿。
  - 风险等级。
  - 是否允许自动发送。
  - 离线托管日报。

2. 权限和安全边界

- Support 角色可以读取客服数据和生成客服草稿。
- GM/老板角色调用客服草稿接口会返回 403。
- 高风险会话仍然必须转人工。
- AI 不允许自动承诺退款、补偿、取消订单、改价或具体到达时间。

3. 中英文显示规则

- 页面中保留必要英文平台名和行业词，但补中文说明。
- 例如：
  - GMV（成交额）。
  - ROAS（广告回报）。
  - CPA（获客成本）。
  - ROI（投入产出）。
  - DM（私信）。
  - Webhook（平台回调）。
  - Human-in-the-loop（人在回路）。
- 客服外语回复草稿保留外语内容，因为跨境客户需要收到英文/法文回复；页面下方增加中文审核说明，方便客服审核。

4. 旧静态 Demo 同步

- `index.html` 仍可作为早期静态 Demo 打开。
- 已同步中文术语到：

```text
index.html
src/app.ts
src/app.js
src/styles.css
```

- 注意：静态 Demo 不能调用 Next API，只适合快速视觉演示。后续正式开发主入口应使用：

```text
http://127.0.0.1:4173
```

### 本次改动文件

```text
app/globals.css
components/growth-app.tsx
index.html
lib/demo-data.ts
src/app.js
src/app.ts
src/styles.css
```

### 验证结果

已通过：

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node scripts/verify-template.mjs
git diff --check
```

API 烟测通过：

- `GET /` 返回 200。
- `GET /api/support/threads` 返回 200。
- `GET /api/support/threads/th_2` 返回 200。
- `POST /api/support/ai-drafts` 使用 GM 角色返回 403，权限拦截正常。

### 本地 Git 提交

已提交：

```text
0ceb0d3 feat: connect support workbench to demo api
```

当前 Git 状态：

```text
干净，无未提交代码改动。
```

### 今日重要结论

- 今天已经完成原“明日计划”里的统一客服工作台 API 接入目标。
- 下一步不建议继续做视觉细节。
- 下一步应进入“客服消息中台持久化准备”：把当前 Demo API 背后的数据结构和真实数据库/权限/审计流程接起来。

## 2026-06-02 客服消息中台持久化准备记录

### 本次目标

- 不继续做 UI 视觉打磨。
- 把统一客服 API 从直接读取 Demo 数据，改为通过 Repository（数据访问层）读取。
- 明确真实模式必须落库的表、审计动作和 AI 审批边界。
- 新增一键客服 API 烟测脚本，方便后续小白团队和 Claude Code 审核。

### 已完成

1. 新增客服数据访问层

新增：

```text
lib/repositories/support-types.ts
lib/repositories/demo-support-repository.ts
lib/repositories/support-repository.ts
```

当前实现仍然是 Demo Repository，但 4 个客服 API 已不再直接读 `getDemoSnapshot()`。

2. 改造客服 API

已改造：

```text
app/api/support/threads/route.ts
app/api/support/threads/[threadId]/route.ts
app/api/support/ai-drafts/route.ts
app/api/support/handoff-report/route.ts
```

现在 API 统一通过：

```text
getSupportRepository()
getSupportActor()
```

读取数据和生成草稿。

3. 补持久化目标和审计事件

API 返回中新增：

```text
persistenceTargets
auditEvents
```

目的：

- 让开发者明确真实模式必须写入哪些表。
- 让 Claude/Codex 审核时能检查是否缺少审计和审批。
- 让后续接 PostgreSQL 时有清晰边界。

4. 补数据库草案

已更新：

```text
db/schema.sql
```

新增/补强：

- `ai_reply_suggestions.can_auto_send`
- `ai_outputs`

原因：

- 前端和 API 已经依赖“是否允许自动发送”，真实落库时不能丢掉该字段。
- AI 输出需要单独记录模型、结果、风险等级和元数据，不能只存在回复草稿表里。

5. 新增客服 API 烟测脚本

新增：

```text
scripts/smoke-support-api.mjs
```

新增命令：

```text
npm run smoke:support
```

覆盖：

- 主页是否可打开。
- 客服会话列表。
- 客服会话详情。
- 客服角色生成高风险 AI 草稿。
- 老板角色不能生成客服草稿。
- 模拟新消息进入客服中台。
- 离线托管日报。

### 验证结果

已通过：

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

说明：

- 第一次跑 `npm run smoke:support` 时主页返回 500，是 `next build` 后本地 dev 服务缓存冲突。
- 已重启 4173 端口 dev 服务。
- 重启后 `npm run smoke:support` 通过 7/7。

### 本地 Git 提交

已提交：

```text
abfb4ed refactor: prepare support message persistence layer
```

### 当前状态

- 当前 Git 工作区将在最终检查后保持干净。
- 当前主入口仍为：

```text
http://127.0.0.1:4173
```

- 旧 `index.html` 仅作为静态过渡 Demo，不作为后续主工程入口。

### 下一步建议

下一步进入：

```text
权限与租户上下文收口
```

状态：已完成，见下方 `2026-06-02 权限与租户上下文收口记录`。

重点：

- 当前 `x-demo-role` 只适合 Demo。
- 下一步要设计真实登录后的 `tenantId`、`userId`、`role`、权限来源。
- API 需要统一从请求上下文读取租户和用户，不能长期使用 Demo 角色头。

## 2026-06-02 权限与租户上下文收口记录

### 本次目标

- 不继续做 UI 视觉打磨。
- 把 Demo 角色、租户 ID、操作者身份集中到统一请求上下文。
- 为后续真实登录、多客户隔离、权限校验和审计日志打基础。
- 保留 `x-demo-role`，但明确只属于 Demo 模式，不作为真实交付认证方案。

### 已完成

1. 新增统一请求上下文

新增：

```text
lib/request-context.ts
```

当前统一输出：

```text
mode：运行模式，当前为 demo（演示模式）
tenantId：客户公司/租户 ID
userId：当前用户 ID
actorRef：审计日志里的操作者标识
role：当前角色
authSource：角色来源，可能是 header（请求头）、cookie（浏览器 Cookie）、default（默认值）
authNote：认证边界说明
```

2. 改造权限入口

已更新：

```text
lib/auth.ts
```

现在 `requirePermission()` 和 `requireAiActionApproval()` 都从统一请求上下文读取角色和租户信息。

403 权限拦截返回中已包含：

```text
mode
tenantId
actorRef
authSource
role
requiredPermission
```

3. 改造客服 Repository 操作者来源

已更新：

```text
lib/repositories/support-repository.ts
lib/repositories/support-types.ts
```

客服中台现在统一使用 `getRequestContext()` 作为操作者上下文，不再在 Repository 里单独拼 Demo 租户和角色。

4. 改造当前用户接口

已更新：

```text
app/api/me/route.ts
```

现在可通过 `/api/me` 查看当前 Demo 上下文、租户、用户、角色、权限和认证边界说明。

5. 增强客服烟测

已更新：

```text
scripts/smoke-support-api.mjs
```

新增检查：

```text
GET /api/me
```

确保统一请求上下文正常返回，并且 support（客服）角色包含 `support.reply` 权限。

### 验证结果

已通过：

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

烟测结果：

```text
客服 API 烟测通过：8/8
```

说明：

- 第一次烟测时首页返回 500，是 `next build` 后本地 dev 服务进程状态冲突。
- 已重启 4173 端口开发服务。
- 重启后首页恢复 200，完整烟测通过。

### 本地 Git 提交

已提交：

```text
9f10abe refactor: centralize demo request context
```

当前主入口：

```text
http://127.0.0.1:4173
```

当前 Git 状态：

```text
干净，无未提交代码改动。
```

### 今日重要结论

- Demo 角色机制已经集中到统一请求上下文，后续不应在业务 API 里散落读取 `x-demo-role`。
- 当前仍是 Demo 模式，没有真实登录，没有真实客户数据，没有真实平台接入。
- 下一步可以进入 AI 审批和审计链路落地准备，让 AI 草稿、人工确认、拒绝、审计记录形成更完整闭环。

## 2026-06-02 AI 审批和审计链路记录

### 本次目标

- 不继续做 UI 视觉打磨。
- 给 AI 回复草稿补人工确认/驳回接口。
- 让 AI 审批结果有结构化返回和审计事件。
- 继续保持 Demo 模式，不真实发送客户消息。

### 已完成

1. 新增 AI 草稿审核接口

新增：

```text
POST /api/support/ai-drafts/review
```

接口输入：

```text
draftId：AI 草稿 ID
decision：approved（确认）或 rejected（驳回）
finalText：人工最终确认文本，可选
reviewNote：审核备注，可选
humanEdited：是否人工改写，可选
```

2. 扩展客服 Repository

已更新：

```text
lib/repositories/support-types.ts
lib/repositories/demo-support-repository.ts
```

新增：

```text
reviewAiDraft()
ReviewAiDraftInput
ReviewAiDraftResult
support.ai_draft.review
```

3. 补数据库草案

已更新：

```text
db/schema.sql
```

`ai_approvals` 增加：

```text
review_note
```

原因：

- 只记录确认/驳回不够。
- 真实客服交接和客户争议时，必须知道为什么确认或驳回。

4. 补 Demo 审批数据

已更新：

```text
lib/types.ts
lib/demo-data.ts
```

`AiApprovalRecord` 增加：

```text
reviewNote
```

5. 扩展客服烟测

已更新：

```text
scripts/smoke-support-api.mjs
```

新增检查：

- 客服角色可以驳回高风险 AI 草稿。
- 老板角色不能审核客服草稿。
- 审核结果必须产生 `support.ai_draft.review` 审计事件。

### 验证结果

已通过：

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

烟测结果：

```text
客服 API 烟测通过：10/10
```

### 本地 Git 提交

已提交：

```text
a3499a9 feat: add ai draft review audit flow
```

当前 Git 状态：

```text
干净，无未提交代码改动。
```

### 当前重要结论

- AI 草稿现在有人工审核接口，不再只是前端概念。
- Demo 模式只模拟审核结果，明确返回 `persisted: false`。
- 高风险草稿即使人工确认，也不会自动发送。
- 下一步不建议马上接真实第三方平台，建议先做 PostgreSQL Repository 最小边界和环境变量护栏。

## 2026-06-02 PostgreSQL Repository 模式护栏记录

### 本次目标

- 不做 UI 视觉改动。
- 不接真实平台。
- 不写真实数据库账号密码。
- 先把 Demo Repository 和未来 PostgreSQL Repository 的切换边界做清楚。
- 防止团队误以为已经接入真实数据库。

### 已完成

1. 新增 Repository 模式状态

已更新：

```text
lib/repositories/support-repository.ts
```

新增：

```text
getSupportRepositoryStatus()
withSupportRepositoryStatus()
```

当前规则：

- 默认 `SUPPORT_REPOSITORY_MODE=demo`。
- 误配成 `postgres` 时仍回退 Demo。
- 当前 PostgreSQL Repository 只是占位，不连接真实数据库。
- 所有客服 API 返回中会附带 `repository` 状态，方便检查当前实际模式。

2. 新增 PostgreSQL 占位 Repository

新增：

```text
lib/repositories/postgres-support-repository.ts
```

说明：

- 只定义未来真实数据库访问层边界。
- 当前不会真正查询或写入数据库。
- 不创建数据库连接，不读取真实数据。

3. 新增 Repository 状态接口

新增：

```text
GET /api/support/repository-status
```

用途：

- 查看请求模式和实际模式。
- 查看是否配置了 `DATABASE_URL`。
- 查看是否启用 PostgreSQL Repository。
- 查看是否发生 Demo 回退和原因。

4. 更新环境变量示例和 README

已更新：

```text
.env.example
README.md
```

明确：

- `.env.example` 只放示例，不是真实密钥。
- 真实 `DATABASE_URL` 只能放 `.env.local` 或部署平台环境变量。
- 当前 `postgres` 模式只是边界预留，仍会回退 Demo。

5. 扩展客服烟测

已更新：

```text
scripts/smoke-support-api.mjs
```

新增检查：

- `GET /api/support/repository-status`。
- `GET /api/support/threads` 返回中包含 `repository.activeMode=demo`。

### 验证结果

已通过：

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

烟测结果：

```text
客服 API 烟测通过：11/11
```

### 本地 Git 提交

已提交：

```text
89f3369 chore: define support repository mode guardrails
```

当前 Git 状态：

```text
干净，无未提交代码改动。
```

### 当前重要结论

- 客服中台已经有清晰的数据访问层模式护栏。
- 当前仍然是 Demo 数据，不是 PostgreSQL 真实落库。
- 下一步可以做本地 PostgreSQL 沙箱落库准备，但只能使用假数据和本地环境，不能接真实客户数据。

## 2026-06-02 本地 PostgreSQL 沙箱准备记录

### 本次目标

- 不做 UI 视觉改动。
- 不接真实平台。
- 不写真实数据库账号密码。
- 为本地 PostgreSQL 沙箱准备 schema、假数据脚本和烟测脚本。
- 保持默认 Demo Repository 可用，不切正式数据库模式。

### 已完成

1. 数据库连接工具懒加载

已更新：

```text
lib/db.ts
```

完成：

- 数据库连接只在调用 `getDb()` 时创建。
- `next build` 不会因为没有数据库而失败。
- 缺少 `DATABASE_URL` 时返回中文错误。
- 新增 `closeDb()`，方便脚本和测试关闭连接。

2. 补用户/角色/权限基础表

已更新：

```text
db/schema.sql
```

新增：

```text
users
roles
permissions
role_permissions
tenant_members
```

原因：

- 后续真实登录、多租户隔离和角色权限需要这些基础表。
- 客服数据不能只挂在 `tenants` 上，必须能追溯到租户成员和角色。

3. 新增 PostgreSQL 沙箱脚本

新增：

```text
scripts/support-postgres-utils.mjs
scripts/check-postgres-schema.mjs
scripts/seed-support-demo-data.mjs
scripts/smoke-support-postgres.mjs
```

新增 npm 命令：

```text
npm run db:schema:check
npm run db:support:seed
npm run smoke:support:postgres
```

脚本用途：

- `db:schema:check`：执行并检查 `db/schema.sql`，确认关键表存在，客服表有 `tenant_id`。
- `db:support:seed`：写入本地假租户、假客户、假客服会话、假 AI 草稿。
- `smoke:support:postgres`：按 `tenant_id` 查询沙箱客服数据，并检查没有真实邮箱域名。

4. 更新 README

已更新：

```text
README.md
```

新增本地 PostgreSQL 沙箱操作顺序：

```text
docker compose up -d postgres
copy .env.example .env.local
npm run db:schema:check
npm run db:support:seed
npm run smoke:support:postgres
```

### 验证结果

已通过：

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/check-postgres-schema.mjs
node --check scripts/seed-support-demo-data.mjs
node --check scripts/smoke-support-postgres.mjs
node --check scripts/support-postgres-utils.mjs
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

默认客服 API 烟测：

```text
客服 API 烟测通过：11/11
```

### 未完成验证

以下 PostgreSQL 沙箱命令未实际跑通：

```text
npm run db:schema:check
npm run db:support:seed
npm run smoke:support:postgres
```

原因：

- 当前没有 `.env.local`。
- Docker 已安装，但 Docker Desktop 后台未启动。
- `docker compose ps postgres` 无法连接 Docker API。

已验证脚本失败提示为中文，不再打印大段堆栈：

```text
PostgreSQL schema 检查未完成。
缺少 DATABASE_URL。请先复制 .env.example 为 .env.local，并确认只使用本地 PostgreSQL 沙箱数据库。
```

### 本地 Git 提交

已提交：

```text
66a9172 chore: prepare local postgres support sandbox
```

当前 Git 状态：

```text
干净，无未提交代码改动。
```

### 当前重要结论

- PostgreSQL 沙箱准备文件和脚本已完成。
- 默认 Demo 模式没有被破坏。
- 真实沙箱落库还需要先启动 Docker Desktop，并创建 `.env.local`。
- 下一步不是继续写业务功能，而是先把本地 PostgreSQL 三步验证跑通。

## 2026-06-02 本地 PostgreSQL 三步验证记录

### 本次目标

- 启动本地 PostgreSQL 沙箱。
- 创建本机专用 `.env.local`。
- 执行数据库建表检查、假数据写入、租户隔离烟测。
- 保持默认 Demo 模式，不切换正式数据库模式。
- 不接真实第三方平台，不写真实客户数据。

### 已完成

1. 启动 Docker Desktop

- 本机 Docker Desktop 已启动。
- 当前项目 PostgreSQL 容器已启动。

2. 处理端口冲突

发现本机 `5432` 端口已经被其他项目占用：

```text
diamond-postgres
```

处理方式：

- 不停止、不修改其他项目容器。
- 当前项目 PostgreSQL 改用本地端口 `55432`。

已同步修改：

```text
docker-compose.yml
.env.example
README.md
```

本机实际数据库地址：

```text
postgres://atomi:atomi@127.0.0.1:55432/atomi_growth
```

3. 创建本机 `.env.local`

- 已从 `.env.example` 复制生成 `.env.local`。
- `.env.local` 只保存在本机。
- `.env.local` 已被 `.gitignore` 忽略，不提交 Git。

4. 跑通 PostgreSQL 三步验证

已通过：

```text
npm run db:schema:check
npm run db:support:seed
npm run smoke:support:postgres
```

烟测结果：

```text
PostgreSQL 客服沙箱烟测通过：5/5
```

验证内容：

- Demo 租户存在。
- 按正确 `tenant_id` 查询能查到客服会话。
- 按错误 `tenant_id` 查询查不到数据。
- 客户邮箱全部是 `.test` 假邮箱。
- AI 草稿和 AI 审批记录存在。

5. 完整工程校验

已通过：

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/check-postgres-schema.mjs
node --check scripts/seed-support-demo-data.mjs
node --check scripts/smoke-support-postgres.mjs
node --check scripts/support-postgres-utils.mjs
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

默认 Demo 客服 API 烟测仍通过：

```text
客服 API 烟测通过：11/11
```

### 本地 Git 提交

已提交：

```text
6e7a212 test: verify local postgres support sandbox
```

当前 Git 状态：

```text
干净，无未提交代码改动。
```

### 当前重要结论

- 本地 PostgreSQL 沙箱已经跑通。
- 当前项目使用 `55432` 端口，避免影响其他项目的 `5432` 数据库。
- 默认 Demo 模式没有被破坏。
- 本机 `.env.local` 没有进入 Git。
- 下一步可以做“只读 PostgreSQL Repository 第一版”，但仍不能接真实客户数据。

## 2026-06-02 只读 PostgreSQL Repository 第一版记录

### 本次目标

- 让统一客服会话列表可以从本地 PostgreSQL 假数据读取。
- 让统一客服会话详情可以从本地 PostgreSQL 假数据读取。
- 让离线托管日报可以从本地 PostgreSQL 假数据读取。
- 默认仍保持 Demo 模式。
- PostgreSQL 模式只读，不允许写真实客户消息、不允许 AI 自动发送。

### 已完成

1. 实现只读 PostgreSQL Repository

已更新：

```text
lib/repositories/postgres-support-repository.ts
lib/repositories/support-repository.ts
lib/repositories/support-types.ts
```

完成：

- `SUPPORT_REPOSITORY_MODE=postgres`
- `ENABLE_POSTGRES_SUPPORT_REPOSITORY=true`
- `DATABASE_URL` 已配置

同时满足以上条件时，客服 Repository 可以进入 PostgreSQL 只读模式。

已支持读取：

- 统一客服会话列表。
- 统一客服会话详情。
- 离线托管日报。

2. 写入接口只读阻断

已更新：

```text
app/api/support/threads/route.ts
app/api/support/ai-drafts/route.ts
app/api/support/ai-drafts/review/route.ts
```

PostgreSQL 模式下，以下动作会返回 `409`，不会写库：

- 模拟新消息进入。
- AI 草稿生成。
- AI 草稿审核。

原因：

- 当前第一版只读。
- 还没有真实登录和正式租户权限。
- 不能让小白团队误以为已经可以写真实客户消息。

3. 新增 PostgreSQL API 烟测

新增：

```text
scripts/smoke-support-postgres-api.mjs
```

新增命令：

```text
npm run smoke:support:postgres-api
```

验证内容：

- Repository 状态进入 PostgreSQL。
- 会话列表来自 PostgreSQL。
- 会话详情来自 PostgreSQL。
- 离线日报来自 PostgreSQL。
- 写入接口被 `409` 阻断。

4. 更新 README

已更新：

```text
README.md
.env.example
package.json
scripts/smoke-support-postgres.mjs
```

说明已改为：

- 默认仍用 Demo。
- PostgreSQL 只读测试建议用 `next build` + `next start` 临时跑 `4174`。
- 不建议把 `.env.local` 默认改成 PostgreSQL 模式。

### 验证结果

已通过：

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/check-postgres-schema.mjs
node --check scripts/seed-support-demo-data.mjs
node --check scripts/smoke-support-postgres.mjs
node --check scripts/smoke-support-postgres-api.mjs
node --check scripts/support-postgres-utils.mjs
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
npm run smoke:support:postgres
npm run smoke:support:postgres-api
```

默认 Demo API：

```text
客服 API 烟测通过：11/11
```

PostgreSQL 数据库直连：

```text
PostgreSQL 沙箱烟测通过：6/6
```

PostgreSQL API：

```text
PostgreSQL API 烟测通过：5/5
```

### 本地 Git 提交

已提交：

```text
a673391 feat: add read-only postgres support repository
```

当前 Git 状态：

```text
干净，无未提交代码改动。
```

### 当前重要结论

- 只读 PostgreSQL Repository 第一版已经完成。
- 默认服务 `http://127.0.0.1:4173` 仍是 Demo 模式。
- 本地 PostgreSQL 沙箱仍运行在 `55432`。
- 这一步没有接真实平台，没有写真实客户数据，没有推 GitHub。
- 下一步建议先做“AI 草稿审核落库 + 审计日志落库”，仍然不做真实客户消息发送。

## 2026-06-02 AI 客服与数据接入工程底座收口记录

### 本次目标

- 继续暂停 UI 视觉打磨。
- 把 AI 客服消息中台从 Demo API 推进到 PostgreSQL 沙箱可验证闭环。
- 把数据接入从静态配置草案推进到 PostgreSQL 沙箱读取。
- 补齐 AI 草稿生成、审核、发送前置检查、审计、渠道接入清单、同步任务护栏。
- 保持默认 Demo 模式，不接真实平台，不发送真实客户消息。

### 今日新增完成

1. PostgreSQL AI 草稿审核落库

- 已完成 PostgreSQL 模式下审核已有 AI 草稿。
- 已更新 `ai_reply_suggestions.status`。
- 已写入 `ai_approvals`。
- 已写入 `audit_logs`。
- 本地提交：

```text
cb79573 feat: persist postgres ai draft reviews
```

2. AI 审批和审计链路增强

- 会话详情已返回 AI 审批记录和审计日志。
- PostgreSQL 读取客服数据时会记录读取审计。
- AI 草稿生成已写入 `ai_reply_suggestions`、`ai_outputs`、`audit_logs`。
- 已修复审计事件 ID 重复问题。
- 本地提交：

```text
fd58c3c feat: expose support approval audit trail
cce8976 feat: persist postgres support read audits
fb22efd feat: persist postgres ai draft generation
bcd2409 fix: make support audit event ids unique
```

3. AI 客服托管闭环汇总

- 新增：

```text
GET /api/support/autopilot-summary
```

- 用于汇总会话、AI 草稿、人工审批、审计日志、离线日报、风险队列。
- 明确返回：
  - AI 自动发送关闭。
  - 高风险自动发送阻断。
  - 当前不写真实客户消息。
- 本地提交：

```text
f845ee5 feat: add support autopilot summary api
```

4. 客服回复发送前置护栏

- 新增：

```text
POST /api/support/replies/send
```

- 当前只做发送资格检查，不发送真实客户消息。
- 高风险、未审核、中高风险草稿会被阻断。
- 低风险已审核草稿也只进入人工发送候选，不真实发送。
- 本地提交：

```text
f27a92e feat: add support reply send guard
```

5. 客服渠道接入前置清单

- 新增：

```text
GET /api/support/channel-readiness
```

- 明确一期先测试：
  - 独立站在线客服。
  - 独立站表单/留言。
  - 邮件客服。
  - 飞书内部通知。
- 企业微信、WhatsApp、抖音 IM、TikTok/其他社媒进入后续官方权限阶段。
- 本地提交：

```text
bcb6814 feat: add support channel readiness api
```

6. 数据接入配置草案与 PostgreSQL 沙箱读取

- 新增/增强：

```text
GET /api/integrations
POST /api/integrations
```

- Demo 模式返回静态配置草案。
- PostgreSQL 模式可从 `integrations` 沙箱表读取 6 个数据接入连接。
- 数据接入探测只返回 Demo 探测结果，不保存账号、不保存密钥、不创建真实连接。
- 本地提交：

```text
1f54575 feat: add integration readiness draft api
231c844 feat: read integration sandbox from postgres
872ae02 feat: add integration probe guardrails
```

7. Demo 同步任务护栏

- 已增强：

```text
POST /api/sync
```

- 返回中明确：
  - `realSyncStarted=false`
  - 排队不等于真实平台同步。
  - 不写真实平台。
  - 不需要真实密钥。
- 本地提交：

```text
9762848 feat: add demo sync guardrails
```

### 当前验证结果

已通过：

```text
npm run typecheck
npm run lint
npm run build
npm run smoke:support
npm run smoke:integrations
npm run smoke:sync
npm run db:schema:check
npm run db:support:seed
npm run smoke:support:postgres
npm run smoke:integrations:postgres-api
npm run smoke:support:postgres-api
node scripts/verify-template.mjs
git diff --check
```

当前烟测结果：

```text
客服 API 烟测通过：16/16
数据接入 API 烟测通过：4/4
同步 API 烟测通过：2/2
PostgreSQL 沙箱烟测通过：8/8
PostgreSQL 数据接入 API 烟测通过：2/2
PostgreSQL API 烟测通过：14/14
```

### 当前 Git 状态

最新本地提交：

```text
872ae02 feat: add integration probe guardrails
```

当前状态：

```text
Git 工作区干净。
未推 GitHub。
未接真实平台。
未写真实密钥。
未发送真实客户消息。
默认服务 4173 仍为 Demo 模式。
```

### 当前审查结论

- 工程方向正确：先做数据模型、权限、审计、沙箱验证，再做真实平台接入。
- 当前完成的是“可验证工程底座”，不是完整商用生产系统。
- 当前最有价值的部分是：AI 客服托管闭环、人工审核边界、PostgreSQL 沙箱、数据接入护栏。
- 当前不适合向客户承诺“已经接通 WhatsApp、抖音、企业微信、TikTok 私信或真实店铺”。
- 当前可以向客户展示“最终平台效果、业务闭环、实施路线、一期试点方式”。

### 下一步入口

下一步先做客户展示 PPT：

```text
D:\SHKF\output\presentations
```

PPT 定位：

- 当面向客户展示。
- 不讲代码细节。
- 重点讲客户最终能看到什么、自动化能解决什么、如何试点、如何保证安全。

## 2026-06-02 客户展示 PPT 收口记录

### 已完成交付

已生成客户当面展示用 PowerPoint：

```text
D:\SHKF\output\presentations\AI电商自动化平台_客户展示方案.pptx
```

PPT 页数：

```text
14 页
```

PPT 定位：

- 面向客户、合作方、内部决策层当面演示。
- 展示 AI 电商自动化平台最终会带来的业务效果。
- 不把当前 Demo 说成已经全平台接通。
- 明确一期先接低难度渠道，后续高权限平台按官方接口推进。
- 明确 AI 客服是托管助手，不替代人工客服。
- 高风险回复、退款、补偿、改价、订单修改等必须人工审核。

### PPT 结构

1. 封面：平台定位与最终交付目标。
2. 演示导航：客户今天会看到的完整业务效果。
3. 当前痛点：复杂业务缺统一判断。
4. 平台总控：每天先看一屏。
5. 统一客服：多平台消息进入一个工作台。
6. 离线托管：下班后 AI 低风险接待与信息收集。
7. 订单物流：客服回复前先看到订单和物流背景。
8. 数据接入：低难度先接，高权限后接。
9. 素材投流：短视频、广告、订单结果形成增长线。
10. 达人 BD：达人建联流程沉淀。
11. 飞书与邮件：协同输出但主控在平台。
12. 交付安全：权限、审计、AI 审批。
13. 10 周路线：MVP 试点节奏。
14. 结论：先用自有业务跑出可验收样板。

### 视觉与内容审查

- 采用暗黑极简、香槟金、高端商务风格。
- 已生成预览图并做 contact sheet 整体检查。
- 已修正第 1 页模拟看板越界问题。
- 已修正标题区和说明文字过近问题。
- PPT 使用中文为主，必要英文已带中文解释，例如：
  - GMV（成交额）
  - ROAS（广告回报）
  - CPA（获客成本）
  - API（接口）
  - SLA（响应时效）
- 没有使用未验证的平台 Logo。
- 没有写入真实密钥、真实客户资料或真实平台接入承诺。

### 当前状态

```text
客户展示 PPT 已生成。
Obsidian 已同步当前收口记录。
GitHub 未推送。
真实平台未接入。
下一步先人工审查 PPT，再继续工程底座。
```

## 2026-06-02 凭证与权限护栏记录

### 本次目标

- 继续暂停 UI 视觉打磨。
- 在真实平台接入前，先补“凭证管理预检”和“权限护栏”。
- 确认接口不会返回真实密钥、环境变量原文、访问凭证、邮箱密码或客户敏感数据。
- 保持 Demo/沙箱边界，不接真实平台、不保存真实密钥、不发送真实客户消息。

### 已完成

1. 新增凭证管理预检接口

新增：

```text
GET /api/integrations/credential-readiness
```

用途：

- 管理员查看服务端加密配置是否具备真实接入前准备条件。
- 客服角色访问会被 403 拦截。
- 接口只返回状态，不返回任何环境变量原文或明文凭证。

2. 增强密钥工具

已更新：

```text
lib/security/secrets.ts
```

新增：

```text
getSecretEncryptionReadiness()
```

当前状态：

- 本地 `SECRETS_ENCRYPTION_KEY` 是占位值。
- 接口显示“已配置但格式无效”是正确状态。
- 真实接入前必须换成 32 字节 base64 加密密钥。
- 当前仍不允许保存真实平台访问凭证。

3. 增强数据接入返回结构

已更新：

```text
GET /api/integrations
```

每个接入项新增：

- 真实上线前置条件。
- 当前禁止动作。
- 凭证策略。

前置条件包括：

- 官方只读接口权限。
- 密钥加密存储。
- 同步日志。
- 失败重试记录。
- 人工审核。
- 操作审计。

4. 同步状态中文化

同步状态增加中文说明：

```text
queued：已排队
running：运行中
succeeded：已完成
failed：失败
skipped：已跳过
```

5. 新增凭证安全烟测

新增：

```text
scripts/smoke-credentials-api.mjs
npm run smoke:credentials
```

验证：

- 管理员能读取凭证预检。
- 客服角色被 403 拦截。
- 接口响应值不包含敏感片段。
- 数据接入配置包含真实上线前置条件。

### 验证结果

已通过：

```text
npm run smoke:credentials
npm run smoke:integrations
npm run smoke:support
npm run smoke:sync
npm run smoke:integrations:postgres-api
npm run smoke:sync:postgres-api
npm run smoke:support:postgres-api
npm run smoke:sync:worker:postgres
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

烟测结果：

```text
凭证安全 API 烟测通过：3/3
数据接入 API 烟测通过：4/4
客服 API 烟测通过：16/16
同步 API 烟测通过：3/3
PostgreSQL 数据接入 API 烟测通过：2/2
PostgreSQL 同步 API 烟测通过：5/5
PostgreSQL API 烟测通过：14/14
PostgreSQL 同步 worker 沙箱烟测通过：2/2
```

### 本地 Git 状态

当前分支：

```text
codex/credential-readiness
```

最新本地提交：

```text
6cb96c1 feat: add credential readiness guardrails
```

当前状态：

```text
Git 工作区干净。
GitHub 未推送。
Obsidian 已同步本节点。
4174 PostgreSQL 沙箱服务已关闭。
4173 默认 Demo 服务保留。
```

### 当前重要结论

- 真实平台接入前的凭证安全状态已经有可检查接口。
- 当前仍不允许保存真实平台凭证。
- 当前不接 Shopify、Meta、TikTok、物流、客服等真实平台。
- 这一步是生产接入前的安全护栏，不是平台真实接入。

### 下一步建议

继续做：

```text
真实登录前权限矩阵与 API 权限自检
```

原因：

- 当前 `x-demo-role` 只适合 Demo。
- 商用前必须能清楚证明每个角色能做什么、不能做什么。
- 在接真实平台前，先把权限矩阵和 API 权限自检做清楚，比提前接平台更安全。

## 2026-06-02 权限矩阵与 API 权限自检记录

### 本次目标

- 继续暂停 UI 视觉打磨。
- 在真实登录系统完成前，先输出当前 Demo 角色权限矩阵。
- 让团队能清楚看到 GM、BD、投手、客服、管理员分别能访问哪些接口。
- 继续明确 `x-demo-role` 只适合 Demo，不代表真实登录。

### 已完成

1. 新增权限矩阵接口

新增：

```text
GET /api/permissions/matrix
```

接口返回：

- 当前请求角色。
- Demo 认证边界。
- 5 个角色的权限矩阵。
- 关键 API 的权限要求。
- 哪些接口需要人工审核。
- 哪些接口不允许真实平台写入。

2. 增强当前用户接口

已更新：

```text
GET /api/me
```

新增：

- `permissionsMatrixEndpoint`
- `authBoundary`

用于提醒团队：

- 当前是 Demo 角色。
- 当前不是真实登录。
- 商用模式必须换成真实登录 Session、租户成员关系和服务端权限校验。

3. 新增权限烟测

新增：

```text
scripts/smoke-permissions-api.mjs
npm run smoke:permissions
```

覆盖：

- 管理员读取权限矩阵。
- 客服也能读取权限矩阵，但不会被标记为真实登录。
- 凭证预检接口只允许管理员。
- AI 客服审核接口需要客服相关权限和人工审核。
- 同步接口不允许真实平台写入。
- `/api/me` 返回权限矩阵入口。

### 验证结果

已通过：

```text
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:integrations
npm run smoke:support
npm run smoke:sync
npm run smoke:sync:worker:postgres
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

烟测结果：

```text
权限矩阵 API 烟测通过：4/4
凭证安全 API 烟测通过：3/3
数据接入 API 烟测通过：4/4
客服 API 烟测通过：16/16
同步 API 烟测通过：3/3
PostgreSQL 同步 worker 沙箱烟测通过：2/2
```

### 本地 Git 状态

当前分支：

```text
codex/credential-readiness
```

最新本地提交：

```text
450fb64 feat: add demo permission matrix guardrails
```

当前状态：

```text
Git 工作区干净。
GitHub 未推送。
4174 PostgreSQL 沙箱服务已关闭。
4173 默认 Demo 服务保留。
```

### 当前重要结论

- 当前 Demo 权限矩阵已经可以通过 API 自检。
- 这一步仍不代表真实登录系统完成。
- 当前最适合继续推进的是“真实登录 Session 与租户授权准备”，不是直接接真实平台。

## 2026-06-02 真实登录 Session 与租户授权准备记录

### 本次目标

- 不接真实登录供应商。
- 不做 UI 视觉打磨。
- 明确当前仍是 Demo Header，不是真实登录 Session。
- 输出真实登录前置条件，让团队知道生产模式不能继续依赖 `x-demo-role`。

### 已完成

1. 新增真实登录准备状态接口

新增：

```text
GET /api/auth/session-readiness
```

接口返回：

- 当前认证来源。
- 当前角色。
- Demo 认证边界。
- 真实登录必须具备的前置条件。
- 生产模式必须禁止的 Demo 输入。

2. 增强当前用户接口

已更新：

```text
GET /api/me
```

新增：

```text
authReadinessEndpoint
```

3. 新增真实登录准备烟测

新增：

```text
scripts/smoke-auth-readiness-api.mjs
npm run smoke:auth-readiness
```

覆盖：

- 管理员读取真实登录准备状态。
- 无效 Demo 角色不会被当成真实登录，会回退为默认 GM。
- `/api/me` 返回真实登录准备入口。

### 验证结果

已通过：

```text
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:integrations
npm run smoke:support
npm run smoke:sync
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

烟测结果：

```text
真实登录准备 API 烟测通过：3/3
权限矩阵 API 烟测通过：4/4
凭证安全 API 烟测通过：3/3
数据接入 API 烟测通过：4/4
客服 API 烟测通过：16/16
同步 API 烟测通过：3/3
```

### 本地 Git 状态

当前分支：

```text
codex/credential-readiness
```

最新本地提交：

```text
1ae0918 feat: add auth readiness guardrails
```

当前状态：

```text
Git 工作区干净。
GitHub 未推送。
4174 PostgreSQL 沙箱服务已关闭。
4173 默认 Demo 服务保留。
```

### 当前重要结论

- 当前已经能清楚说明 Demo Header 和真实登录之间的差距。
- 生产环境不能使用 `x-demo-role` 推导权限。
- 下一步最合理的是做 PostgreSQL RBAC 权限读取沙箱，让权限逐步从静态配置走向数据库表验证。

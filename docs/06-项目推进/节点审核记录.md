---
type: project
status: active
phase: v1
owner: project-lead
tags: [review, checkpoint]
---

# 节点审核记录

## 审核模板

### 日期

### 本周目标

### 已完成

### 未完成

### 是否跑偏

### 发现的问题

### 风险等级

### 调整策略

### 下周重点

---

## 2026-06-05 启动审核

- 状态：待审核。
- 审核范围：项目边界、团队职责、GitHub/Obsidian 协作规则、一期交付范围。

## 2026-06-05 收工状态复核审核

### 审核范围

- 2026-06-03 RBAC / Session 准备层未提交改动。
- 当前 Git 分支、最近提交基线、未提交文件数量。
- 最近验证结果是否能从现有记录正式确认。
- 收工文档是否把“代码存在”“构建痕迹存在”和“验证通过”明确区分。

### 审核结论

- 方向未跑偏，但今天仍没有新增工程提交，本轮仍属于“状态复核审核”，不属于“功能收口审核”。
- 当前仍不能确认 `smoke:rbac`、`smoke:rbac:postgres`、`smoke:session-adapter`、`smoke:session-context` 已实际执行并通过。
- 当前也不能把 2026-06-03 的 `.next` 构建产物时间和脚本时间当成正式验证结论。
- 当前不适合提交 Git，也不适合对外表达成真实登录准备已经验证完成。

### 已完成

- 已确认当前分支仍是 `codex/credential-readiness`。
- 已确认最近已提交基线仍是 `1ae0918 feat: add auth readiness guardrails`。
- 已确认当前仍有 19 个未提交文件：
  - 8 个已跟踪修改。
  - 11 个新增文件。
- 已确认 `git diff --stat` 仍是 8 个已跟踪文件，约 230 行新增、43 行删除。
- 已确认最近验证相关文件时间仍集中在 2026-06-03 11:04 至 12:29。
- 已完成 Obsidian 收工同步：
  - `每日工作推进计划`
  - `明日计划`
  - `风险与决策`
  - `节点审核记录`

### 未完成

- 未确认新增 smoke 已执行并通过。
- 未确认 `typecheck / lint / build / verify-template / git diff --check` 已对当前这批改动重跑通过。
- 未提交 Git。
- 未安装真实登录依赖。

### 是否跑偏

- 未跑偏。
- 今天没有回到 UI 打磨，也没有越过安全边界去接真实平台。
- 当前问题仍然是验证滞后，不是路线错误。

### 发现的问题

- 现有记录最多只能确认“脚本和接口存在”“2026-06-03 出现过本地构建痕迹”，不能确认“新增验证已完成”。
- 当前未提交状态已经连续跨越两次收工，如果明天继续叠加功能，会继续扩大收口难度。
- `docs/真实登录Session与权限落地方案.md` 仍在未提交状态，后续如果代码再变但文档不跟，容易出现说明失真。

### 风险等级

- 中风险偏高。

### 调整策略

- 2026-06-06 开工先不扩功能，先执行：

```text
git status --short
git diff --stat
git log --oneline -5
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

- 只有全部留痕后，才考虑本地提交：

```text
feat: add rbac and session readiness guardrails
```

### 下次审核重点

- 新增 smoke 是否全部通过并被明确记录。
- PostgreSQL RBAC 假数据是否与权限矩阵一致。
- Session 适配层和上下文边界是否继续保持 Demo/真实分离。
- 当前 19 个未提交文件是否被收口到可提交状态。

## 2026-06-04 收工状态核对审核

### 审核范围

- 2026-06-03 RBAC / Session 准备层未提交改动。
- 当前 Git 分支、最近提交基线、未提交文件数量。
- 最近验证结果是否能从现有记录确认。
- 收工文档是否把“已落地”和“已验证”明确区分。

### 审核结论

- 方向未跑偏，但今天没有新增工程提交，本轮属于“状态核对审核”，不属于“功能收口审核”。
- 当前仍不能确认 2026-06-03 新增 smoke 已实际执行并通过。
- 当前不适合提交 Git，也不适合对外表达成真实登录准备已经验证完成。

### 已完成

- 已确认当前分支仍是 `codex/credential-readiness`。
- 已确认最近已提交基线仍是 `1ae0918 feat: add auth readiness guardrails`。
- 已确认当前仍有 19 个未提交文件：
  - 8 个已跟踪修改。
  - 11 个新增文件。
- 已确认最新文件改动时间集中在 2026-06-03 11:04 至 12:27。
- 已完成 Obsidian 收工同步：
  - `每日工作推进计划`
  - `明日计划`
  - `风险与决策`
  - `节点审核记录`

### 未完成

- 未确认 `smoke:rbac`、`smoke:rbac:postgres`、`smoke:session-adapter`、`smoke:session-context` 已执行并通过。
- 未确认 `typecheck / lint / build / verify-template / git diff --check` 已对这批改动重跑通过。
- 未提交 Git。
- 未安装真实登录依赖。

### 是否跑偏

- 未跑偏。
- 今天没有回到 UI 打磨，也没有越过安全边界去接真实平台。
- 当前唯一问题是验证滞后，不是方向错误。

### 发现的问题

- Obsidian 目前只能确认“代码存在”和“文件未提交”，不能确认“新增验证已完成”。
- 当前工作区悬而未决时间已经跨到 2026-06-04 收工，如果明天继续叠加功能，会让未提交面和审核难度继续扩大。
- `docs/真实登录Session与权限落地方案.md` 仍在未提交状态，后续如代码先变、文档后补，容易失真。

### 风险等级

- 中风险偏高。

### 调整策略

- 2026-06-05 开工先不扩功能，先执行：

```text
git status --short
git diff --stat
git log --oneline -5
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

- 只有全部留痕后，才考虑本地提交：

```text
feat: add rbac and session readiness guardrails
```

### 下次审核重点

- 新增 smoke 是否全部通过并被明确记录。
- PostgreSQL RBAC 假数据是否与权限矩阵一致。
- Session 适配层和上下文边界是否继续保持 Demo/真实分离。
- 当前 19 个未提交文件是否被收口到可提交状态。

## 2026-06-03 RBAC / Session 准备层收工审核

### 审核范围

- PostgreSQL RBAC 准备状态接口。
- Session 适配层准备状态接口。
- Session 上下文准备状态接口。
- 权限矩阵与种子数据补强。
- 今日新增烟测脚本和 `package.json` 脚本入口。

### 审核结论

- 方向通过，但今天只确认到“代码与脚本已落地”，还不能确认“完整验证已通过”。
- 当前推进没有跑偏，仍然围绕真实登录前的权限边界、租户隔离和统一上下文做准备。
- 当前不适合提交 Git，也不适合对外说“真实登录已完成”。

### 已完成

- 已新增 `GET /api/permissions/rbac-readiness`。
- 已新增 `GET /api/auth/session-adapter-readiness`。
- 已新增 `GET /api/auth/session-context-readiness`。
- 已新增：
  - `lib/auth/session-adapter.ts`
  - `lib/auth/session-context.ts`
  - `lib/rbac/postgres-rbac-readiness.ts`
- 已新增烟测：
  - `npm run smoke:rbac`
  - `npm run smoke:rbac:postgres`
  - `npm run smoke:session-adapter`
  - `npm run smoke:session-context`
- 已新增本地方案文档：
  - `docs/真实登录Session与权限落地方案.md`

### 未完成

- 未确认今天新增烟测已经执行并通过。
- 未确认 `typecheck / lint / build / verify-template / git diff --check` 已对这批改动重跑通过。
- 未提交 Git。
- 未接真实登录供应商。

### 是否跑偏

- 未跑偏。
- 今天继续先补权限边界和验证入口，优先级高于直接装 Auth.js 或接真实平台。

### 发现的问题

- 当前 Obsidian 只能确认新增代码和脚本存在，不能确认今天已经跑出新的验证结果。
- 如果明天跳过验证直接提交，会让仓库基线和收工记录都不可靠。
- 当前分支仍是 `codex/credential-readiness`，最近已提交基线仍是 `1ae0918`，说明今天工作还停留在未提交状态。

### 风险等级

- 中风险。

### 调整策略

- 明天开工先不扩功能，先执行：

```text
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
npm run smoke:auth-readiness
npm run smoke:permissions
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

- 通过后再决定是否本地提交：

```text
feat: add rbac and session readiness guardrails
```

### 下次审核重点

- PostgreSQL RBAC 假数据是否和静态权限矩阵一致。
- 错误租户是否仍然读不到成员权限。
- Session 适配层是否继续保持 Demo/真实边界清楚。
- 是否在不安装真实登录依赖的前提下，把统一请求上下文和权限来源先收口干净。

## 2026-06-02 工程底座临时审核

### 本周目标

- 项目启动。
- 建立 Git checkpoint。
- 暂停 UI 细节打磨，转入 AI 客服消息中台工程底座。

### 已完成

- 已创建本地 checkpoint：`830b459 chore: checkpoint demo state before backend foundation`。
- 已补客服消息中台数据库草案。
- 已补客服会话、消息、翻译、AI 草稿、AI 审批、离线日报类型。
- 已补模拟数据。
- 已补客服权限。
- 已补 Demo API 骨架。

### 未完成

- 未接真实平台。
- 未接真实数据库持久化。
- 未把统一客服页面接入 API。

### 是否跑偏

- 未跑偏。
- 今天从 UI 转向工程底座是正确调整，符合一期目标。

### 发现的问题

- 当前 API 只是模拟接口，还没有数据库持久化。
- `npm audit` 提示 Next/PostCSS 依赖链中危风险，但强制修复方案不适合执行。
- 下一步如果继续做静态页面，会偏离工程闭环。

### 风险等级

- 中风险。

### 调整策略

- 已安装依赖并跑完整校验。
- 已提交工程底座：`565f096 feat: add support message hub backend skeleton`。
- 下一步只做统一客服功能闭环，不继续视觉细节。
- PostCSS 中危风险暂不强制修复，等待 Next 官方升级路径。

### 下周重点

- 完成 AI 客服消息中台最小闭环。
- 补数据库持久化或明确 Demo/真实模式切换方案。
- 开始统一客服工作台的数据接入。

## 2026-06-01 数据接入页临时审核

### 本周目标

- 优化数据接入页视觉。
- 建立暗黑轻奢后台基调。
- 保留 Demo/真实模式、安全边界、风险等级和连接状态。

### 已完成

- 已生成 Figma v2 数据接入页设计稿。
- 已同步到本地 Demo。
- 已恢复风险等级颜色：低风险绿色、中风险橙色、高风险红色。
- 已恢复数据接入页侧边栏安全边界。
- 已恢复接入卡片 `Demo` / `已连接` 状态反馈。

### 是否跑偏

- 轻微跑偏，已调整。
- 跑偏点：过度追求极简，导致风险颜色、业务状态和安全边界变弱。

### 发现的问题

- 中风险不能混用 Demo 金色。
- 数据接入页不能隐藏 Demo/真实模式。
- Figma 视觉稿不能直接替代后台业务判断。

### 风险等级

- 中风险。

### 调整策略

- 继续保留暗黑轻奢风。
- 所有风险、权限、审批、连接状态必须显性显示。
- 明天先做人工 UI 审查，再决定是否提交 Git。

### 下周重点

- 完成数据接入页最后一轮商用化调整。
- 开始统一客服工作台和 AI 托管设置页面。

## 2026-06-02 晚间功能闭环审核

### 审核范围

- 统一客服工作台 Demo API 接入。
- 页面英文术语中文解释。
- 旧静态 Demo 与 Next 主工程一致性。
- 权限与高风险 AI 回复边界。

### 审核结论

- 通过本轮功能闭环审核。
- 统一客服工作台已经可以读取 Demo API，会话列表、详情、翻译、AI 草稿、风险等级和离线托管日报都能展示。
- GM/老板角色生成客服草稿会被 403 拦截，权限边界正常。
- 高风险客服回复仍然不能自动发送。
- 页面中关键英文术语已补中文解释。

### 已验证

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node scripts/verify-template.mjs
git diff --check
```

API 烟测：

```text
GET /
GET /api/support/threads
GET /api/support/threads/th_2
POST /api/support/ai-drafts with gm role -> 403
```

### 本地提交

```text
0ceb0d3 feat: connect support workbench to demo api
```

### 下次审核重点

- 客服消息是否开始真实落库。
- API 是否通过统一权限层，不只是前端控制。
- AI 草稿、人工审批和审计日志是否形成闭环。
- 旧静态 Demo 是否可以逐步降级为展示文件，避免继续双线维护。

## 2026-06-02 持久化准备审核

### 审核范围

- 客服 Repository 数据访问层。
- 客服 API 路由是否从直接 Demo 数据读取改为统一访问层。
- 数据库草案是否补齐 AI 自动发送状态和 AI 输出记录。
- 客服 API 烟测是否可一键运行。

### 审核结论

- 通过本轮持久化准备审核。
- 客服 API 已统一走 `getSupportRepository()`。
- Demo 数据读取已集中到 `lib/repositories/demo-support-repository.ts`。
- 每个客服 API 返回中包含持久化目标和审计事件，后续更容易接 PostgreSQL。
- 新增固定烟测命令 `npm run smoke:support`。

### 已验证

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

烟测结果：

```text
客服 API 烟测通过：7/7
```

### 本地提交

```text
abfb4ed refactor: prepare support message persistence layer
```

### 下次审核重点

- 租户上下文是否建立。
- 真实登录用户和角色是否能替代 `x-demo-role`。
- Repository 是否能平滑替换为 PostgreSQL 实现。
- `audit_logs` 是否能真实记录 actor、event、metadata。

## 2026-06-02 权限上下文收口审核

### 审核范围

- 统一请求上下文。
- 权限拦截返回信息。
- 客服 Repository 操作者来源。
- `/api/me` 当前用户/权限自检接口。
- 客服 API 烟测覆盖。

### 审核结论

- 通过本轮权限上下文收口审核。
- 已新增 `lib/request-context.ts`，集中输出 `mode`、`tenantId`、`userId`、`actorRef`、`role`、`authSource`。
- `lib/auth.ts` 的权限拦截已统一读取请求上下文。
- 客服中台 Repository 已不再单独拼接 Demo 租户和角色。
- `/api/me` 已能返回当前上下文和权限列表，方便后续排查角色问题。

### 已验证

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

烟测结果：

```text
客服 API 烟测通过：8/8
```

### 本地提交

```text
9f10abe refactor: centralize demo request context
```

### 下次审核重点

- AI 草稿的人工审批是否能形成明确记录。
- `ai_approvals` 和 `audit_logs` 是否有真实落库前的接口边界。
- 中高风险 AI 回复是否仍然不能自动发送。
- Demo 模式和未来真实登录模式是否继续保持清晰边界。

## 2026-06-02 AI 审批和审计链路审核

### 审核范围

- AI 回复草稿人工确认/驳回接口。
- `ai_approvals` 审核记录字段。
- 审计事件 `support.ai_draft.review`。
- 客服角色和老板角色权限边界。
- 客服 API 烟测覆盖。

### 审核结论

- 通过本轮 AI 审批链路审核。
- 已新增 `POST /api/support/ai-drafts/review`。
- Demo Repository 可以模拟确认/驳回，但明确返回 `persisted: false`。
- 老板角色不能审核客服草稿。
- 高风险草稿不会自动发送。
- 审核动作会产生 `support.ai_draft.review` 审计事件。

### 已验证

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

烟测结果：

```text
客服 API 烟测通过：10/10
```

### 本地提交

```text
a3499a9 feat: add ai draft review audit flow
```

### 下次审核重点

- PostgreSQL Repository 边界是否清晰。
- 真实数据库连接是否被环境变量护栏保护。
- Demo Repository 和真实 Repository 是否能清楚切换。
- 是否继续保持不接真实第三方平台、不写真实密钥的原则。

## 2026-06-02 Repository 模式护栏审核

### 审核范围

- Demo Repository 和 PostgreSQL Repository 模式切换。
- 环境变量护栏。
- Repository 状态接口。
- 客服 API 返回中的模式状态。
- `.env.example` 和 README 说明。

### 审核结论

- 通过本轮 Repository 模式护栏审核。
- 默认继续使用 Demo Repository。
- PostgreSQL Repository 当前只是占位实现，不连接真实数据库。
- 误配 `SUPPORT_REPOSITORY_MODE=postgres` 时不会静默进入真实模式。
- 客服烟测已覆盖 Repository 状态。

### 已验证

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

烟测结果：

```text
客服 API 烟测通过：11/11
```

### 本地提交

```text
89f3369 chore: define support repository mode guardrails
```

### 下次审核重点

- 是否可以用本地假数据跑通 PostgreSQL 沙箱落库。
- 数据库连接是否懒加载，不能在 `next build` 阶段崩溃。
- 是否按 `tenant_id` 查询和写入。
- 是否保留 Demo 回退能力。

## 2026-06-02 PostgreSQL 沙箱准备审核

### 审核范围

- 数据库连接懒加载。
- 用户/角色/权限基础表。
- PostgreSQL schema 检查脚本。
- 沙箱假数据种子脚本。
- PostgreSQL 沙箱烟测脚本。
- 默认 Demo 模式是否保持可用。

### 审核结论

- 通过代码层审核。
- `next build` 不依赖数据库，说明连接保持懒加载。
- 默认客服 API 烟测仍通过 11/11。
- 已新增本地 PostgreSQL 沙箱三步脚本。
- 实际 PostgreSQL 落库验证未完成，原因是本机 Docker Desktop 未启动且没有 `.env.local`。

### 已验证

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/check-postgres-schema.mjs
node --check scripts/seed-support-demo-data.mjs
node --check scripts/smoke-support-postgres.mjs
node --check scripts/support-postgres-utils.mjs
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

### 未完成验证

```text
npm run db:schema:check
npm run db:support:seed
npm run smoke:support:postgres
```

原因：

- Docker Desktop 后台未启动。
- 缺少 `.env.local`。

### 本地提交

```text
66a9172 chore: prepare local postgres support sandbox
```

### 下次审核重点

- 启动 Docker Desktop 后，是否能完整跑通本地 PostgreSQL 三步验证。
- schema 是否能无错误执行。
- 假数据是否能按 `tenant_id` 查询。
- 沙箱数据是否没有真实客户信息。

## 2026-06-02 PostgreSQL 沙箱验证审核

### 审核范围

- Docker Desktop 启动状态。
- PostgreSQL 容器是否能正常运行。
- 本地数据库端口是否与其他项目冲突。
- `.env.local` 是否只保留在本机。
- schema 建表检查、假数据写入、租户隔离烟测。
- 默认 Demo API 是否仍然可用。

### 审核结论

- 通过本轮 PostgreSQL 沙箱验证审核。
- 已发现 `5432` 端口被其他项目 `diamond-postgres` 占用。
- 当前项目已改用 `55432`，没有停止或修改其他项目容器。
- 已用本地假数据跑通 PostgreSQL 三步验证。
- 默认 Demo 模式没有被破坏。
- `.env.local` 没有提交 Git。

### 已验证

```text
docker compose ps postgres
npm run db:schema:check
npm run db:support:seed
npm run smoke:support:postgres
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/check-postgres-schema.mjs
node --check scripts/seed-support-demo-data.mjs
node --check scripts/smoke-support-postgres.mjs
node --check scripts/support-postgres-utils.mjs
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
```

PostgreSQL 沙箱烟测结果：

```text
PostgreSQL 客服沙箱烟测通过：5/5
```

默认客服 API 烟测结果：

```text
客服 API 烟测通过：11/11
```

### 本地提交

```text
6e7a212 test: verify local postgres support sandbox
```

### 下次审核重点

- PostgreSQL Repository 第一版是否只做只读查询。
- 是否严格按 `tenant_id` 查询。
- 默认 Demo 模式是否继续保留。
- `next build` 是否仍不依赖数据库连接。
- 是否没有真实客户数据、真实密钥和真实平台消息。

## 2026-06-02 只读 PostgreSQL Repository 审核

### 审核范围

- PostgreSQL Repository 是否能读取本地假数据。
- 会话列表是否按租户读取。
- 会话详情是否能读取消息、翻译、AI 草稿。
- 离线托管日报是否能读取。
- PostgreSQL 模式是否阻断写入动作。
- 默认 Demo 模式是否没有被破坏。

### 审核结论

- 通过本轮只读 PostgreSQL Repository 审核。
- 会话列表、会话详情、离线日报已经可以从本地 PostgreSQL 沙箱读取。
- PostgreSQL 模式写入接口已返回 `409` 阻断，避免误写客户消息。
- 默认 4173 服务仍然是 Demo 模式。
- 本轮没有接真实平台，没有写真实客户数据，没有推 GitHub。

### 已验证

```text
npm run typecheck
npm run lint
npm run build
node --check src/app.js
node --check scripts/check-postgres-schema.mjs
node --check scripts/seed-support-demo-data.mjs
node --check scripts/smoke-support-postgres.mjs
node --check scripts/smoke-support-postgres-api.mjs
node --check scripts/support-postgres-utils.mjs
node --check scripts/smoke-support-api.mjs
node scripts/verify-template.mjs
git diff --check
npm run smoke:support
npm run smoke:support:postgres
npm run smoke:support:postgres-api
```

烟测结果：

```text
客服 API 烟测通过：11/11
PostgreSQL 沙箱烟测通过：6/6
PostgreSQL API 烟测通过：5/5
```

### 本地提交

```text
a673391 feat: add read-only postgres support repository
```

### 下次审核重点

- AI 草稿审核落库是否只更新沙箱数据。
- 是否写入 `ai_approvals` 和 `audit_logs`。
- 高风险草稿是否仍然不会自动发送。
- 默认 Demo 模式是否继续可回退。
- 是否仍然没有真实客户数据和真实平台消息。

## 2026-06-02 AI 客服与数据接入底座收口审核

### 审核范围

- PostgreSQL AI 草稿生成、审核、审计日志落库。
- 客服会话读取审计。
- AI 客服托管闭环汇总。
- 客服回复发送前置护栏。
- 客服渠道接入前置清单。
- 数据接入 Demo 配置草案。
- 数据接入 PostgreSQL 沙箱读取。
- Demo 同步任务护栏。
- 默认 Demo 模式是否仍然可用。

### 审核结论

- 通过本轮工程底座审核。
- 当前已经具备“AI 客服消息中台 + 人工审核 + 审计 + PostgreSQL 沙箱 + 数据接入护栏”的可验证底座。
- 当前没有接真实平台，没有写真实密钥，没有导入真实客户数据，没有发送真实客户消息。
- 当前适合进入客户展示材料制作，但展示话术必须强调“一期试点、沙箱验证、官方权限逐步接入”。
- 当前不适合承诺全渠道一期全部真实打通。

### 已验证

```text
npm run typecheck
npm run lint
npm run build
npm run smoke:support
npm run smoke:integrations
npm run smoke:sync
npm run db:schema:check
npm run db:support:seed
npm run smoke:support:postgres
npm run smoke:integrations:postgres-api
npm run smoke:support:postgres-api
node scripts/verify-template.mjs
git diff --check
```

烟测结果：

```text
客服 API 烟测通过：16/16
数据接入 API 烟测通过：4/4
同步 API 烟测通过：2/2
PostgreSQL 沙箱烟测通过：8/8
PostgreSQL 数据接入 API 烟测通过：2/2
PostgreSQL API 烟测通过：14/14
```

### 本地提交

本轮收口后最新提交：

```text
872ae02 feat: add integration probe guardrails
```

本轮关键提交：

```text
cb79573 feat: persist postgres ai draft reviews
fd58c3c feat: expose support approval audit trail
cce8976 feat: persist postgres support read audits
fb22efd feat: persist postgres ai draft generation
f845ee5 feat: add support autopilot summary api
f27a92e feat: add support reply send guard
bcb6814 feat: add support channel readiness api
1f54575 feat: add integration readiness draft api
9762848 feat: add demo sync guardrails
231c844 feat: read integration sandbox from postgres
872ae02 feat: add integration probe guardrails
```

### 本轮发现的问题

- Obsidian 的明日计划一度落后于实际工程进度，已在本次同步中修正。
- `next build` 后默认 dev 服务偶尔会因构建缓存返回首页 500，需要重启 `4173` dev 服务后复测。
- PostgreSQL 沙箱和 Demo 模式均可用，但真实登录、多租户授权、真实平台接入尚未完成。

### 下次审核重点

- 客户展示 PPT 是否把“已完成底座”和“未来真实接入”区分清楚。
- 是否避免对客户承诺一期全平台全部接通。
- 下一轮如继续开发，应优先做同步日志表、失败重试记录、真实接入前的密钥加密方案。
- 如果准备推 GitHub，必须先确认 Git 状态干净、无真实密钥、Obsidian 已同步。

## 2026-06-02 客户展示 PPT 审核记录

### 审核对象

```text
D:\SHKF\output\presentations\AI电商自动化平台_客户展示方案.pptx
```

### 审核结论

- 可以作为客户当面演示初版。
- 页数为 14 页，符合控制在 15 页左右的要求。
- 叙事顺序清楚：痛点、最终平台效果、AI 客服托管、订单物流、数据接入、增长模块、安全交付、试点路线。
- 文案以客户价值为主，没有堆代码细节。
- 已明确一期不承诺全平台全部接通。
- 已明确高风险 AI 回复必须人工审核。
- 已明确当前展示为 Demo/试点路线，不是生产环境已全面上线。

### 需要人工再看的点

- 客户名称、品牌名称、真实业务案例是否需要加入。
- 是否需要替换为客户自己的腕表图片或真实业务截图。
- 客户是否更关心“降本增效”还是“销售增长”，后续可按客户关注点调整演示重点。

### 下一步建议

1. 先由项目负责人当面过一遍 PPT 讲述节奏。
2. 如客户要看真实操作，再打开当前 Demo 后台补充演示。
3. PPT 审查通过后，继续工程底座：数据同步日志表 + 同步任务审计。

## 2026-06-02 凭证与权限护栏审核记录

### 审核范围

- 凭证管理预检接口。
- 密钥加密配置状态检查。
- 数据接入真实上线前置条件。
- 同步状态中文说明。
- 敏感信息是否通过接口响应外露。
- 默认 Demo 与 PostgreSQL 沙箱是否仍可用。

### 审核结论

- 通过本轮凭证与权限护栏审核。
- 管理员可以读取凭证管理预检状态。
- 客服角色读取凭证管理预检会被 403 拦截。
- 接口响应值未发现 `SECRETS_ENCRYPTION_KEY`、占位加密密钥、平台访问凭证、邮箱密码等敏感内容。
- 数据接入接口已返回真实上线前置条件，但仍不保存真实密钥、不创建真实平台连接。
- 同步状态已经有中文说明，降低小白团队误解风险。

### 已验证

```text
npm run smoke:credentials
npm run smoke:integrations
npm run smoke:support
npm run smoke:sync
npm run smoke:integrations:postgres-api
npm run smoke:sync:postgres-api
npm run smoke:support:postgres-api
npm run smoke:sync:worker:postgres
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

验证结果：

```text
凭证安全 API 烟测通过：3/3
数据接入 API 烟测通过：4/4
客服 API 烟测通过：16/16
同步 API 烟测通过：3/3
PostgreSQL 数据接入 API 烟测通过：2/2
PostgreSQL 同步 API 烟测通过：5/5
PostgreSQL API 烟测通过：14/14
PostgreSQL 同步 worker 沙箱烟测通过：2/2
```

### 本地提交

```text
6cb96c1 feat: add credential readiness guardrails
```

当前分支：

```text
codex/credential-readiness
```

### 本轮发现的问题

- Obsidian 旧计划仍记录 `872ae02`，已在本次同步中修正为当前分支最新提交 `6cb96c1`。
- `next build` 后 dev 服务可能出现缓存状态不稳定，凭证接口曾短暂返回异常；重启 4173/4174 后验证通过。
- 本地加密密钥仍是占位值，真实接入前必须重新生成有效 32 字节 base64 密钥。

### 下次审核重点

- API 权限矩阵是否清楚列出每个角色能访问哪些接口。
- `x-demo-role` 是否继续只作为 Demo 入口，不被误认为真实登录。
- 真实平台接入前是否已经补齐密钥加密、权限、同步日志、失败重试、审计和人工审核。

## 2026-06-02 权限矩阵与 API 权限自检审核记录

### 审核范围

- Demo 角色权限矩阵接口。
- `/api/me` 当前用户权限入口。
- 关键 API 权限边界。
- Demo 角色与真实登录边界。
- 高风险接口是否继续标记人工审核和禁止真实平台写入。

### 审核结论

- 通过本轮权限矩阵审核。
- 已新增 `GET /api/permissions/matrix`。
- 权限矩阵包含 GM、BD、投手、客服、管理员 5 个角色。
- 凭证预检接口明确只允许管理员角色。
- AI 客服审核接口明确需要客服相关权限并保持人工审核。
- 同步接口明确不允许真实平台写入。
- `/api/me` 已返回权限矩阵入口和 Demo 认证边界。

### 已验证

```text
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:integrations
npm run smoke:support
npm run smoke:sync
npm run smoke:sync:worker:postgres
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

验证结果：

```text
权限矩阵 API 烟测通过：4/4
凭证安全 API 烟测通过：3/3
数据接入 API 烟测通过：4/4
客服 API 烟测通过：16/16
同步 API 烟测通过：3/3
PostgreSQL 同步 worker 沙箱烟测通过：2/2
```

### 本地提交

```text
450fb64 feat: add demo permission matrix guardrails
```

当前分支：

```text
codex/credential-readiness
```

### 下次审核重点

- 是否定义真实登录 Session 与 Demo Header 的切换边界。
- 是否能从真实用户、租户成员、角色关系推导权限。
- 是否能避免真实生产环境继续依赖 `x-demo-role`。

## 2026-06-02 真实登录准备状态审核记录

### 审核范围

- 真实登录准备状态接口。
- `/api/me` 认证准备入口。
- 无效 Demo 角色处理。
- 真实登录前置条件说明。
- 生产模式禁止项。

### 审核结论

- 通过本轮真实登录准备状态审核。
- 已新增 `GET /api/auth/session-readiness`。
- 接口明确当前不是真实登录，不创建真实 Session。
- 接口列出真实模式必须具备：正式登录 Session、租户成员表、角色表、权限表、角色权限表、服务端权限校验和审计 actor。
- 接口明确生产模式必须禁用 `x-demo-role`、`demo_role Cookie`、固定 Demo 租户和前端自报角色。

### 已验证

```text
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:integrations
npm run smoke:support
npm run smoke:sync
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

验证结果：

```text
真实登录准备 API 烟测通过：3/3
权限矩阵 API 烟测通过：4/4
凭证安全 API 烟测通过：3/3
数据接入 API 烟测通过：4/4
客服 API 烟测通过：16/16
同步 API 烟测通过：3/3
```

### 本地提交

```text
1ae0918 feat: add auth readiness guardrails
```

当前分支：

```text
codex/credential-readiness
```

### 下次审核重点

- PostgreSQL RBAC 权限读取是否能从 `tenant_members`、`roles`、`permissions`、`role_permissions` 推导权限。
- Demo 静态权限和数据库权限之间是否有清楚边界。
- 真实登录供应商接入前是否已经有权限读取测试和租户隔离测试。

## 2026-06-03 Session 与 RBAC 准备审核记录

### 审核范围

- Session 适配层草案。
- Session 上下文映射草案。
- PostgreSQL RBAC 准备状态接口。
- RBAC 沙箱种子数据。
- 今日验证是否已有正式记录。

### 审核结论

- 方向通过，但本轮只算“准备层审核通过”，不算真实登录交付完成。
- 已新增：
  - `GET /api/auth/session-adapter-readiness`
  - `GET /api/auth/session-context-readiness`
  - `GET /api/permissions/rbac-readiness`
- 已明确推荐路线为：

```text
Auth.js + PostgreSQL RBAC
```

- 已明确当前真实 Session 仍未启用，业务 API 仍依赖统一请求上下文和 Demo 回退。
- 已确认 PostgreSQL 种子脚本补齐了 5 个 `.test` 假用户、5 个角色和完整角色权限映射。

### 当前未完成

- 还没有安装真实登录依赖。
- 还没有把真实 Session 接入业务 API。
- 还没有从今日记录中确认新 smoke 已经实际跑通。
- 还没有新的本地 Git 提交。

### 已确认的最近验证

只能确认到 2026-06-02 已记录通过的：

```text
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:integrations
npm run smoke:support
npm run smoke:sync
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

今天新增的以下验证入口：

```text
npm run smoke:session-adapter
npm run smoke:session-context
npm run smoke:rbac
npm run smoke:rbac:postgres
```

当前只能确认“已写入脚本和命令”，还不能确认“已实际跑通”。

### 本轮发现的问题

- Obsidian 的明日计划如果不更新，明天开工容易继续沿用昨天已完成节点。
- readiness 接口和说明文档已经比较完整，但容易被误解成真实登录已落地。
- 当前工作区不是干净状态，明天必须先做 smoke 验证和收口，不适合直接继续加功能。

### 下次审核重点

- `smoke:session-adapter`、`smoke:session-context`、`smoke:rbac`、`smoke:rbac:postgres` 是否全部跑通。
- RBAC 沙箱是否持续保证只有 `.test` 假用户。
- 管理员之外的角色是否仍被 `rbac-readiness` 正常拦截。
- 在真正安装 Auth.js 前，是否继续保持 Demo 模式和统一上下文边界清晰。

---
type: tomorrow-plan
status: active
phase: v1
owner: project-lead
date: 2026-06-06
project_root: D:\腕表工程文件\as腕表ai工作流app开发
tags: [tomorrow-plan, backend-foundation, customer-service, typecheck]
---

# 明日计划

## 当前接续计划（2026-06-05 收工同步后）

### 当前状态

今天没有新增工程提交，主要完成的是仓库状态复核和 Obsidian 收工同步。

当前仍停留在 2026-06-03 已写入但未提交的 3 个方向：

- PostgreSQL RBAC 准备状态接口与沙箱烟测入口。
- Session 适配层准备状态接口与烟测入口。
- Session 上下文映射准备状态接口与烟测入口。

当前仓库状态：

```text
分支：codex/credential-readiness
最近已提交基线：1ae0918 feat: add auth readiness guardrails
当前有 19 个未提交文件（8 个已跟踪修改 + 11 个新增文件）
```

今天已经再次确认方向没跑偏，但最近可见的“验证痕迹”仍主要是 2026-06-03 的脚本修改时间和本地构建产物时间，不能当成 smoke 已正式通过的记录。本节为 2026-06-06 开工时的唯一最新入口。

### 明天总目标

继续做：

```text
RBAC / Session 准备层验证收口
```

重点不是再扩功能，而是先把 2026-06-03 已写出来的接口、脚本、假数据和说明文档补成可追溯验证记录。

### 明天第 1 步：先核对基线

先打开：

```text
D:\SHKF\docs\06-项目推进\每日工作推进计划.md
D:\SHKF\docs\06-项目推进\明日计划.md
D:\SHKF\docs\06-项目推进\风险与决策.md
D:\SHKF\docs\06-项目推进\节点审核记录.md
```

再检查工程：

```text
git status --short
git diff --stat
git log --oneline -5
```

应重点确认：

- 当前分支仍是 `codex/credential-readiness`。
- 最近已提交基线仍是 `1ae0918 feat: add auth readiness guardrails`。
- 当前 19 个未提交文件仍集中在 RBAC / Session 准备层。

### 明天第 2 步：先跑新增 smoke

先执行：

```text
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
```

如果 PostgreSQL 沙箱未启动，再补：

```text
npm run db:schema:check
npm run db:support:seed
```

### 明天第 3 步：再补完整校验

新增 smoke 通过后，再执行：

```text
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

### 明天第 4 步：重点确认什么

需要重点确认：

- `/api/permissions/rbac-readiness` 能证明 PostgreSQL 假数据中的角色、权限、租户成员已就位。
- `/api/auth/session-adapter-readiness` 明确当前仍是 Demo provider，真实 Session 未启用。
- `/api/auth/session-context-readiness` 明确未来真实 Session 必须提供 `userId / tenantId / role`，并由 PostgreSQL RBAC 提供权限。
- 管理员之外的角色仍被 RBAC 管理自检接口正常拦截。
- 错误租户仍查不到成员权限。
- `docs/真实登录Session与权限落地方案.md` 与代码当前边界一致，没有提前承诺真实登录已上线。

### 明天第 5 步：通过后再决定是否本地提交

如果以上验证都通过，再考虑本地提交，建议提交信息：

```text
feat: add rbac and session readiness guardrails
```

提交前仍必须确认：

- 不含真实密钥。
- 不含真实客户数据。
- 不接真实登录供应商。
- Obsidian 已同步。

### 明天第 6 步：收工时必须补写什么

如果明天完成验证，收工记录必须明确写清：

- 哪些 smoke 已实际执行并通过。
- `typecheck / lint / build / verify-template / git diff --check` 是否全部通过。
- 当前 19 个未提交文件是否缩减。
- 是否满足本地提交条件。
- 如果仍未通过，卡在哪一个命令，不要写模糊结论。

### 明天不要做什么

- 不直接安装 Auth.js、Clerk、Auth0、NextAuth。
- 不把准备状态接口写成“真实登录已完成”。
- 不接真实平台。
- 不导入真实用户或真实客户消息。
- 不在未验证前提交 Git 或推 GitHub。

## 当前接续计划（2026-06-04 收工同步后）

### 当前状态

今天没有新增工程提交，主要完成的是状态核对和收工同步。

当前仍停留在 2026-06-03 已写入但未提交的 3 个方向：

- PostgreSQL RBAC 准备状态接口与沙箱烟测入口。
- Session 适配层准备状态接口与烟测入口。
- Session 上下文映射准备状态接口与烟测入口。

当前仓库状态：

```text
分支：codex/credential-readiness
最近已提交基线：1ae0918 feat: add auth readiness guardrails
当前有 19 个未提交文件（8 个已跟踪修改 + 11 个新增文件）
```

今天已经再次确认方向没跑偏，但还不能从记录里确认这批改动已经跑过完整验证。本节为 2026-06-05 开工时的唯一最新入口。

### 明天总目标

继续做：

```text
RBAC / Session 准备层校验收口
```

重点不是再扩功能，而是先把 2026-06-03 已写出来的接口、脚本和假数据校验跑通并留痕。

### 明天第 1 步：先核对基线

先打开：

```text
D:\SHKF\docs\06-项目推进\每日工作推进计划.md
D:\SHKF\docs\06-项目推进\明日计划.md
D:\SHKF\docs\06-项目推进\风险与决策.md
D:\SHKF\docs\06-项目推进\节点审核记录.md
```

再检查工程：

```text
git status --short
git diff --stat
git log --oneline -5
```

应重点确认：

- 当前分支仍是 `codex/credential-readiness`。
- 最近已提交基线仍是 `1ae0918 feat: add auth readiness guardrails`。
- 当前 19 个未提交文件仍集中在 RBAC / Session 准备层。

### 明天第 2 步：只补验证，不先扩功能

先执行：

```text
npm run smoke:rbac
npm run smoke:rbac:postgres
npm run smoke:session-adapter
npm run smoke:session-context
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

如果 PostgreSQL 沙箱未启动，再补：

```text
npm run db:schema:check
npm run db:support:seed
```

### 明天第 3 步：确认今天这批改动是否真的成立

需要重点确认：

- `/api/permissions/rbac-readiness` 能证明 PostgreSQL 假数据中的角色、权限、租户成员已就位。
- `/api/auth/session-adapter-readiness` 明确当前仍是 Demo provider，真实 Session 未启用。
- `/api/auth/session-context-readiness` 明确未来真实 Session 必须提供 `userId / tenantId / role`，并由 PostgreSQL RBAC 提供权限。
- 客服角色仍不能读取 RBAC 管理自检接口。
- 错误租户不能读到成员权限。
- `docs/真实登录Session与权限落地方案.md` 与代码当前边界一致，没有提前承诺真实登录已上线。

### 明天第 4 步：通过后再决定是否本地提交

如果以上验证都通过，再考虑本地提交，建议提交信息：

```text
feat: add rbac and session readiness guardrails
```

提交前仍必须确认：

- 不含真实密钥。
- 不含真实客户数据。
- 不接真实登录供应商。
- Obsidian 已同步。

### 明天第 5 步：收工时必须补写什么

如果明天完成验证，收工记录必须明确写清：

- 哪些 smoke 已实际执行并通过。
- `typecheck / lint / build / verify-template / git diff --check` 是否全部通过。
- 当前 19 个未提交文件是否缩减。
- 是否满足本地提交条件。
- 如果仍未通过，卡在哪一个命令，不要写模糊结论。

### 明天不要做什么

- 不直接安装 Auth.js、Clerk、Auth0、NextAuth。
- 不把准备状态接口写成“真实登录已完成”。
- 不接真实平台。
- 不导入真实用户或真实客户消息。
- 不在未验证前提交 Git 或推 GitHub。

## 当前接续计划（2026-06-02 真实登录准备完成后）

### 当前状态

当前工程已经完成 AI 客服消息中台、PostgreSQL 沙箱、AI 审核审计、数据接入护栏、同步 worker 审计沙箱、凭证管理预检护栏、Demo 权限矩阵、真实登录准备状态。当前最新分支和提交为：

```text
分支：codex/credential-readiness
提交：1ae0918 feat: add auth readiness guardrails
```

本节为当前最新计划。下面旧的接续计划保留为历史记录，不再作为下一步执行入口。

### 下一步总目标

继续做：

```text
PostgreSQL RBAC 权限读取沙箱
```

原因：

- 当前权限矩阵仍来自静态 `lib/permissions.ts`。
- 数据库里已经有 `tenant_members`、`roles`、`permissions`、`role_permissions`。
- 下一步应验证能否从 PostgreSQL 假数据读取角色和权限，为真实登录做准备。

### 只做这一个闭环

- 不接真实登录供应商。
- 不接真实平台。
- 只用本地 PostgreSQL 假数据验证 RBAC。
- 增加一个只读权限来源检查接口或脚本。
- 验证正确租户能读到角色权限。
- 验证错误租户读不到权限。
- 验证默认 Demo 权限仍可回退。

### 暂时不做

- 不引入 Clerk/Auth0/NextAuth 等真实登录方案。
- 不保存真实密钥。
- 不导入真实用户或客户数据。
- 不推 GitHub。

### 建议验收

```text
npm run smoke:auth-readiness
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run db:schema:check
npm run db:support:seed
npm run smoke:support:postgres
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

## 当前接续计划（2026-06-02 权限矩阵完成后）

### 当前状态

当前工程已经完成 AI 客服消息中台、PostgreSQL 沙箱、AI 审核审计、数据接入护栏、同步 worker 审计沙箱、凭证管理预检护栏、Demo 权限矩阵。当前最新分支和提交为：

```text
分支：codex/credential-readiness
提交：450fb64 feat: add demo permission matrix guardrails
```

本节为当前最新计划。下面旧的“当前接续计划”和“当前明日计划”保留为历史记录，不再作为下一步执行入口。

### 下一步总目标

继续做：

```text
真实登录 Session 与租户授权准备
```

原因：

- 当前权限矩阵已经能说明 Demo 角色能做什么。
- 下一步要把“Demo Header”和“未来真实登录”边界拆清楚。
- 在接真实平台前，必须先定义真实登录后如何得到 `tenantId`、`userId`、`role` 和权限。

### 下一步只做这一个闭环

- 增加真实登录准备状态接口或扩展 `/api/me` 的真实模式缺口说明。
- 明确 Demo Header 只在 Demo 模式可用。
- 明确真实模式必须来自登录 Session、租户成员表、角色权限表。
- 明确生产环境禁止使用 `x-demo-role` 推导权限。
- 新增烟测，证明当前仍是 Demo，且接口不会把 Demo 说成真实登录。

### 暂时不做

- 不接真实登录供应商。
- 不接真实 Shopify、Meta、TikTok、物流、客服平台。
- 不保存真实密钥。
- 不发送真实客户消息。
- 不推 GitHub。

### 验收标准

建议验证：

```text
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:support
npm run smoke:integrations
npm run smoke:sync
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

## 当前接续计划（2026-06-02 晚间同步后）

### 当前状态

当前工程已经完成 AI 客服消息中台、PostgreSQL 沙箱、AI 审核审计、数据接入护栏、同步 worker 审计沙箱、凭证管理预检护栏。客户展示 PPT 已生成。当前最新分支和提交为：

```text
分支：codex/credential-readiness
提交：6cb96c1 feat: add credential readiness guardrails
```

本节为当前最新计划。下面旧的“当前明日计划”保留为历史记录，不再作为下一步执行入口。

### 下一步总目标

继续做：

```text
真实登录前权限矩阵与 API 权限自检
```

原因：

- 当前 `x-demo-role` 仍是 Demo 入口。
- 商用交付前必须清楚证明每个角色能访问哪些接口、不能做哪些高风险动作。
- 先把权限边界做清楚，再接真实平台，风险更低。

### 下一步优先处理

建议优先处理：

```text
lib/permissions.ts
app/api/me/route.ts
scripts/smoke-support-api.mjs
scripts/smoke-integrations-api.mjs
README.md
```

可新增：

```text
GET /api/permissions/matrix
scripts/smoke-permissions-api.mjs
npm run smoke:permissions
```

### 只做这一个闭环

- 输出当前 Demo 角色权限矩阵。
- 明确 GM、BD、投手、客服、管理员分别能访问哪些功能。
- 明确哪些动作必须人工审核。
- 明确 `x-demo-role` 只用于 Demo。
- 验证客服不能管理数据接入和凭证预检。
- 验证 GM/老板不能生成或审核客服回复草稿。
- 验证管理员可以管理数据接入，但仍不能保存真实密钥或创建真实连接。

### 暂时不做

- 不接真实 Shopify、Meta、TikTok、物流、客服平台。
- 不接 WhatsApp、抖音、企业微信、TikTok 私信。
- 不做真实登录系统。
- 不保存真实密钥。
- 不发送真实客户消息。
- 不推 GitHub。

### 验收标准

必须验证：

```text
npm run smoke:permissions
npm run smoke:credentials
npm run smoke:integrations
npm run smoke:support
npm run smoke:sync
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
git diff --check
```

如涉及 PostgreSQL 沙箱，再验证：

```text
npm run smoke:integrations:postgres-api
npm run smoke:support:postgres-api
npm run smoke:sync:postgres-api
```

### 下一步不要做什么

- 不继续 UI 视觉打磨。
- 不把 Demo 权限说成真实登录权限。
- 不把凭证预检说成已经能接真实平台。
- 不导入真实客户数据。
- 不自动发送 AI 回复。
- 不在未确认前推 GitHub。

## 当前明日计划（2026-06-03 再更新）

### 明天总目标

当前 AI 客服消息中台、PostgreSQL 沙箱、AI 审核审计、数据接入护栏已经完成一轮工程底座收口。客户展示 PPT 已生成。明天优先做两件事：先审查客户展示 PPT 的表达是否准确，再继续做“数据同步日志表 + 同步任务审计”第一版。仍然不接真实平台，不导入真实客户数据，不自动发送客户消息。

注意：本文件后面保留了早期 UI 阶段计划作为历史记录。明天开工以本节“当前明日计划”为准。

### 明天第 1 步：读取资料

先打开 Obsidian：

```text
D:\SHKF\docs\06-项目推进\每日工作推进计划.md
D:\SHKF\docs\06-项目推进\风险与决策.md
D:\SHKF\docs\06-项目推进\节点审核记录.md
D:\SHKF\docs\06-项目推进\周计划.md
```

再检查工程：

```text
D:\腕表工程文件\as腕表ai工作流app开发
git status --short
git log --oneline -5
```

当前最新本地提交应为：

```text
872ae02 feat: add integration probe guardrails
```

### 明天第 2 步：确认当前基线

先确认默认 Demo 服务正常：

```text
http://127.0.0.1:4173
npm run smoke:support
```

再确认 PostgreSQL 沙箱正常：

```text
docker compose ps postgres
npm run smoke:support:postgres
npm run smoke:integrations:postgres-api
```

### 明天第 3 步：先审查客户展示 PPT

重点审查：

```text
D:\SHKF\output\presentations\AI电商自动化平台_客户展示方案.pptx
```

检查标准：

- 客户是否能看懂最终平台价值。
- 是否清楚区分“当前沙箱能力”和“未来真实接入”。
- 是否没有承诺一期全渠道全部接通。
- 是否能体现高端跨境腕表电商场景。
- 是否能展示电脑端后台、手机端工作台、AI 客服托管、订单物流、数据接入、飞书/邮件协同。
- 是否适合当面讲解，而不是像技术文档。

### 明天第 4 步：继续工程开发建议

建议下一轮开发：

```text
数据同步日志表 + 同步任务审计
```

优先处理：

```text
db/schema.sql
app/api/sync/route.ts
scripts/seed-support-demo-data.mjs
scripts/smoke-support-postgres.mjs
scripts/smoke-sync-api.mjs
README.md
```

只做这一个闭环：

- 新增或补强同步日志表。
- Demo 同步任务写入沙箱审计或返回明确审计目标。
- 记录 provider、状态、请求人、开始时间、结束时间、失败原因、重试次数。
- 仍然不触发真实平台同步。
- 仍然不写真实密钥。
- 仍然不导入真实客户数据。

暂时不做：

- 不接真实 Shopify、Meta、TikTok、物流、客服平台。
- 不接 WhatsApp、抖音、企业微信、TikTok 私信。
- 不做自动发邮件给客户。
- 不把排队任务说成真实同步完成。

### 明天第 5 步：验收标准

必须验证：

- 默认 Demo API 仍通过。
- 数据接入 API 仍通过。
- 同步 API 仍通过。
- PostgreSQL 沙箱直连仍通过。
- PostgreSQL 数据接入 API 仍通过。
- 高风险草稿仍不能自动发送。
- 数据同步日志不包含真实客户信息和真实密钥。
- `next build` 不依赖数据库启动。

建议命令：

```text
npm run typecheck
npm run lint
npm run build
npm run smoke:support
npm run smoke:integrations
npm run smoke:sync
npm run smoke:support:postgres
npm run smoke:integrations:postgres-api
npm run smoke:support:postgres-api
node scripts/verify-template.mjs
git diff --check
```

### 明天第 6 步：是否提交 Git

如果验证通过，建议本地提交：

```text
feat: add sync audit sandbox
```

提交后同步 Obsidian。

### 明天不要做什么

- 不继续 UI 视觉打磨。
- 不接真实平台。
- 不导入真实客户消息。
- 不自动发送 AI 回复。
- 不把 PostgreSQL 模式设为默认。
- 不在未确认前推 GitHub。

---

## 历史计划（2026-06-03：AI 草稿审核落库，已完成）

### 原目标

在只读 PostgreSQL Repository 已完成的基础上，做“AI 草稿审核落库 + 审计日志落库”第一版。

### 完成状态

已完成，并在后续继续推进了 AI 草稿生成落库、读取审计、审批审计、发送前置护栏、渠道接入清单、数据接入沙箱读取和连接探测护栏。

关键提交：

```text
cb79573 feat: persist postgres ai draft reviews
fd58c3c feat: expose support approval audit trail
cce8976 feat: persist postgres support read audits
fb22efd feat: persist postgres ai draft generation
872ae02 feat: add integration probe guardrails
```

---

## 历史计划（2026-06-03：只读 PostgreSQL Repository，已完成）

### 明天总目标

在已经跑通本地 PostgreSQL 沙箱的基础上，做“只读 PostgreSQL Repository 第一版”。目标是让客服会话列表和会话详情可以从本地假数据库读取，但默认仍保持 Demo 模式，避免小白团队误以为已经进入真实生产环境。

### 明天第 1 步：读取资料

先打开 Obsidian：

```text
D:\SHKF\docs\06-项目推进\每日工作推进计划.md
D:\SHKF\docs\06-项目推进\风险与决策.md
D:\SHKF\docs\06-项目推进\节点审核记录.md
D:\SHKF\docs\06-项目推进\周计划.md
```

再检查工程：

```text
D:\腕表工程文件\as腕表ai工作流app开发
git status --short
git log --oneline -5
```

当前最新本地提交应为：

```text
6e7a212 test: verify local postgres support sandbox
```

### 明天第 2 步：确认本地沙箱状态

先确认 Docker Desktop 已启动，再执行：

```text
docker compose ps postgres
npm run smoke:support:postgres
```

必须看到：

```text
PostgreSQL 客服沙箱烟测通过：5/5
```

如果 Docker 没启动，先启动 Docker Desktop。不要停止或修改其他项目的数据库容器。

### 明天第 3 步：实现只读 PostgreSQL Repository

优先处理：

```text
lib/repositories/postgres-support-repository.ts
lib/repositories/support-repository.ts
lib/db.ts
scripts/smoke-support-postgres.mjs
README.md
```

只做两个只读能力：

- 会话列表：按 `tenant_id` 读取客服会话。
- 会话详情：按 `tenant_id` 和 `thread_id` 读取消息、翻译、AI 草稿。

暂时不做：

- 真实平台接入。
- 真实客户消息写入。
- AI 自动发送。
- 订单修改、退款、取消订单。
- 将默认模式切到 PostgreSQL。

### 明天第 4 步：验证 Demo 回退没有被破坏

每次修改后运行：

```text
npm run typecheck
npm run lint
npm run build
npm run smoke:support
npm run smoke:support:postgres
node scripts/verify-template.mjs
git diff --check
```

重点检查：

- 默认 Demo API 仍通过 `11/11`。
- PostgreSQL 沙箱仍通过 `5/5`。
- `next build` 不依赖数据库连接。
- `.env.local` 不进入 Git。
- 没有真实客户数据。

### 明天第 5 步：是否提交 Git

如果验证通过，建议本地提交：

```text
feat: add read-only postgres support repository
```

提交后继续同步 Obsidian。

### 明天不要做什么

- 不继续 UI 视觉打磨。
- 不接真实 WhatsApp、抖音、企业微信、TikTok 私信。
- 不写真实 token、secret、邮箱密码。
- 不导入真实客户消息。
- 不把 `SUPPORT_REPOSITORY_MODE` 默认改为 `postgres`。
- 不在未确认前推 GitHub。

---

## 历史计划（2026-06-03 原定：PostgreSQL 三步验证，已完成）

### 明天总目标

基于已经完成的本地 PostgreSQL 沙箱脚本，下一步先跑通“本地 PostgreSQL 三步验证”。仍然不做 UI 视觉打磨，也不接真实第三方平台，只用本地假数据验证 schema、种子数据和租户隔离查询。

### 明天第 1 步：读取资料

先打开：

```text
D:\SHKF\docs\06-项目推进\每日工作推进计划.md
D:\SHKF\docs\06-项目推进\风险与决策.md
D:\SHKF\docs\06-项目推进\节点审核记录.md
```

再检查工程：

```text
D:\腕表工程文件\as腕表ai工作流app开发
git status --short
```

### 明天第 2 步：确认工程基线

重点看：

- 最新本地提交是否存在：

```text
66a9172 chore: prepare local postgres support sandbox
```

- Git 状态是否干净。
- 本地服务是否能打开：

```text
http://127.0.0.1:4173
```

### 明天第 3 步：跑通本地 PostgreSQL 三步验证

目标：

- 不再继续扩展 UI 视觉。
- 不接真实平台消息。
- 不写真实数据库账号密码。
- 启动 Docker Desktop。
- 创建 `.env.local`。
- 用本地假数据验证 PostgreSQL（数据库）表结构。
- 确认所有客服表读写都按 `tenant_id` 查询。
- 保留 Demo 回退能力，不直接切换正式模式。

建议先处理：

```text
README.md
.env.example
docker-compose.yml
db/schema.sql
scripts/check-postgres-schema.mjs
scripts/seed-support-demo-data.mjs
scripts/smoke-support-postgres.mjs
```

必须按顺序执行：

```text
docker compose up -d postgres
copy .env.example .env.local
npm run db:schema:check
npm run db:support:seed
npm run smoke:support:postgres
```

如果 `docker compose up -d postgres` 失败，先不要继续写数据库代码，记录失败原因。

仍然只能使用：

```text
本地 docker postgres
本地 .env.local
假客户数据
```

明天重点确认：

- Docker Desktop 是否已启动。
- `.env.local` 是否存在且没有真实生产密码。
- `db:schema:check` 是否通过。
- `db:support:seed` 是否写入假数据。
- `smoke:support:postgres` 是否能按 `tenant_id` 查询。
- 默认 `npm run smoke:support` 是否仍然通过。

明天的最小交付标准：

- 本地 PostgreSQL 三步命令能跑通，或明确记录 Docker/环境问题。
- 能证明假数据可以落到 PostgreSQL。
- 能证明错误 `tenant_id` 查不到客服会话。
- Demo 模式继续默认可用，`npm run smoke:support` 继续通过。
- 不把真实 `DATABASE_URL` 写入 Git。
- 文档能说清楚本地沙箱和真实生产数据库的区别。

### 明天第 4 步：保留安全边界

必须保留：

- Demo/真实模式提示。
- 高风险人工审核。
- 中高风险不能自动发送。
- GM 角色不能直接生成客服回复草稿。
- `x-demo-role` 只能用于 Demo。
- 不接真实 WhatsApp、抖音、企业微信或个人微信。
- 不把真实 token、secret、邮箱密码写进前端。
- 不把真实 `DATABASE_URL` 写入 Git。
- 不把本地沙箱数据说成真实客户数据。
- 不让旧静态 Demo 成为后续主工程入口。

### 明天第 5 步：验证

```text
npm run typecheck
npm run lint
npm run build
node scripts/verify-template.mjs
npm run smoke:support
git diff --check
```

同时烟测：

```text
GET /
GET /api/support/threads
GET /api/support/threads/th_2
POST /api/support/ai-drafts
POST /api/support/ai-drafts/review
GET /api/support/repository-status
```

如果本地 PostgreSQL 三步验证跑通，可以本地提交。

建议提交信息：

```text
test: verify local postgres support sandbox
```

### 明天第 6 步：是否推 GitHub

明天如果你确认要让 3 人团队或 Claude Code 继续接手，可以考虑推送到 GitHub 私有仓库。

推送前必须确认：

- Git 状态干净。
- 本地校验通过。
- 没有真实密钥。
- 没有真实客户数据。
- Obsidian 已同步。

不建议未审核就直接推。

### 明天不要做什么

- 不继续深度打磨 UI。
- 不接真实 WhatsApp、抖音、企业微信或个人微信。
- 不写真实 token、secret、邮箱密码。
- 不让 AI 自动发送高风险客户消息。
- 不在未确认前推 GitHub。
- 不继续大规模维护旧静态 Demo，除非为了过渡演示必须同步。

---

## 历史计划（2026-06-02）

## 日期

2026-06-02

## 明天总目标

先审查今天的数据接入页改动，确认它是否适合商用后台和客户演示。确认后再决定是否提交 Git，然后进入下一个核心页面：统一客服工作台 + AI 托管设置。

## 明天第 1 步：打开资料

### 操作

1. 打开 Obsidian。
2. 进入：

```text
D:\SHKF\docs\06-项目推进
```

3. 先看这两个文件：

- [[每日工作推进计划]]
- [[2026-06-01-数据接入页与风险颜色审查记录]]

4. 再打开 Demo：

```text
D:\腕表工程文件\as腕表ai工作流app开发\index.html
```

## 明天第 2 步：人工审查当前页面

### 审查页面

- 数据接入
- 统一客服工作台
- AI 确认中心

### 审查重点

- 页面是否比早期版本更清楚。
- 风险颜色是否一眼能识别。
- 数据接入页是否既高级又不丢业务判断。
- Demo / 真实模式是否清楚。
- 连接状态是否清楚。
- 客户是否能理解当前只是 Demo，不是已经接通真实平台。

## 明天第 3 步：数据接入页补强

如果页面确认方向没问题，继续补以下内容。

### 1. 每个接入卡片增加接入难度

建议分级：

- 低难度：CSV、表单、邮件、飞书内部通知
- 中难度：Shopify / 独立站、物流 API、Meta Ads、TikTok Ads
- 高难度：WhatsApp、企业微信、抖音 IM、TikTok 私信

### 2. 增加真实上线前置条件

建议显示：

- 必须登录。
- 必须区分客户公司。
- 必须加密保存密钥。
- 必须有同步日志。
- 必须有失败重试记录。
- 必须有操作审计。

### 3. 保留当前已恢复的内容

不能再删：

- 风险等级颜色。
- Demo / 真实模式。
- 岗位权限。
- 连接状态。
- 高风险人工审核提示。

## 明天第 4 步：校验

每次修改后运行：

```text
node --check src/app.js
node scripts/verify-template.mjs
git diff --check
```

如果要运行 TypeScript 检查，需要先安装依赖；这一步明天再决定。

## 明天第 5 步：是否提交 Git

如果人工审查通过，再提交。

建议提交信息：

```text
feat: refine data integration ui and restore risk signals
```

提交前确认：

- 页面能打开。
- 风险颜色正常。
- 数据接入状态正常。
- 没有真实密钥。
- 没有真实客户数据。
- Obsidian 已同步。

## 明天第 6 步：进入下一个核心模块

建议下一步做：

```text
统一客服工作台 + AI 托管设置
```

原因：

- 这是整个项目最核心的客户价值。
- 可以直接展示多平台消息统一接收。
- 可以展示 AI 离线托管。
- 可以展示低风险自动回复和高风险转人工。
- 可以展示客服上班后的日报汇总。

## 明天给 Codex 的接续提示

```text
继续 AI 电商自动化平台 Demo。

先读取：
D:\SHKF\docs\06-项目推进\每日工作推进计划.md
D:\SHKF\docs\06-项目推进\明日计划.md
D:\SHKF\docs\06-项目推进\2026-06-01-数据接入页与风险颜色审查记录.md

再检查工程：
D:\腕表工程文件\as腕表ai工作流app开发

当前有未提交改动。不要直接提交，先人工审查页面。

重点：
- 暗黑轻奢风可以保留。
- 风险等级颜色不能弱化。
- Demo/真实模式不能隐藏。
- 数据接入状态不能固定成 Demo。
- 商用后台必须可判断、可操作、可审计。

审查通过后，再补数据接入难度和真实上线前置条件。
最后再决定是否提交 Git。
```
